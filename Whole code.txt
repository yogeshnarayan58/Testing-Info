I want to test ONE controlled fix for the Cycode SAST finding in servey-mod/e2e.

Use the COMPLETE current code I provide for:
- e2e/utils/safe-path.ts
- e2e/playwright/reporters/lt-session-reporter.ts
- e2e/playwright/setup/globalTeardown.ts
- e2e/utils/base-page-factory.ts
- configurationDetails.ts, specifically getLambdaTestSessionDir()

Do NOT modify any files yet.

First, analyze the current implementation and then provide the COMPLETE replacement code only for the LambdaTest session-path flow.

Experiment we want to test:

Instead of a generic helper such as:

safeJoin(baseDir, fileName)

where the trusted root is supplied as a function parameter, create a purpose-specific LambdaTest path helper where the trusted root is internally derived from the existing getLambdaTestSessionDir() / existing configuration behavior.

Example concept only:

safeLambdaTestSessionPath(fileName)

The existing security protections MUST remain:
- null-byte rejection
- traversal rejection
- ../ and ~ rejection
- backslash rejection
- absolute/UNC/Windows drive-path rejection
- filename character allowlist
- realpath/symlink protection
- containment verification
- existing .json/filename validation where applicable

IMPORTANT:
- Do NOT hardcode ".lt-sessions" if getLambdaTestSessionDir() does anything else.
- Preserve the exact existing behavior of getLambdaTestSessionDir().
- Do NOT change .gitignore.
- Do NOT change the .lt-sessions directory behavior.
- Do NOT remove any security check.
- Do NOT modify Allure handling yet.
- Do NOT modify globalTeardown or base-page-factory yet unless required for the LambdaTest experiment.
- Do NOT introduce unused variables.
- Do NOT move existing try/catch boundaries.
- Do NOT change error-handling behavior.
- Do NOT invent missing code.

The goal is specifically to test whether Cycode treats a purpose-specific helper with an internally trusted LambdaTest root differently from the current generic helper that accepts the root as a parameter.

After reviewing the supplied code, give me:
1. The complete replacement safe-path.ts required for this experiment.
2. The exact minimal change required in lt-session-reporter.ts to use it.
3. A short explanation of why each change is being made.
4. TypeScript commands to compile/check the change before running Cycode.

Do NOT claim this will definitely fix Cycode. This is a controlled experiment. If the supplied code shows that this approach would introduce a regression, say so instead of generating code.