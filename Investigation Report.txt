Investigation Report — Positive "next screen" signal for searchAndSelectDataset()
I read the four files as they currently exist (no edits, no git commands run). Here's what's available.
const isOnDatasetScreen = await chooseDatasetBreadcrumb.isVisible();
if (!isOnDatasetScreen) {
  return true; // assumes "already past this screen" — the flagged risk
}
Current buggy pattern (both page objects)
What happens right after a dataset is selected
In both files, the method called immediately after searchAndSelectDataset() (applyEntireDataset() in start-from-scratch.page.ts and applyDataset() in start-with-prompt.page.ts) checks a variable literally named isChooseViewDialogShown, built from the dialogElement resource:
const dialogElement = this.webActionHandler.getWebElement(
  this.resourceManager.getResource('dialogElement', ...)
);
const isChooseViewDialogShown = await dialogElement.isVisible().catch(() => false);

That naming is the strongest existing clue in the codebase for what the "next screen" is (a "Choose a view" dialog).

Existing resources found (from the locator JSON files)
Both start-from-scratch.locator.json and start-with-prompt.locator.json define identical entries for these two candidates:
elementName	selectorType	selectorValue	Used as
dialogElement	css	[data-test-id='title-second-dialog']	Checked right after dataset click, variable named isChooseViewDialogShown — the best candidate for a positive "next screen" signal
applyButton	css	[data-test-id='apply-dataset-btn']	Clicked on that same next screen to confirm/apply the dataset or view
Additional resources present but not currently wired into this flow:

entireDatasetOption (start-from-scratch only): [data-test-id='rv-default-dataview'] — an option that would presumably only exist/be visible on the "choose a view" screen, so it could also corroborate arrival at the next screen.
selectPanelistsHeading (start-from-scratch only): text=Select Panelists — this looks like a screen further downstream, not the immediate next screen after dataset selection, so it's likely not the right signal here.
Bottom line
Best existing candidate: dialogElement ([data-test-id='title-second-dialog']) — already named/used elsewhere in these same files as the "choose view" indicator (isChooseViewDialogShown).
Secondary corroborating candidate: applyButton ([data-test-id='apply-dataset-btn']) — present on that same next screen.
Nothing new needs to be invented — both locator files already define these two resources identically, so no live DOM check is required to find a signal; it already exists in the codebase, just not used inside searchAndSelectDataset() itself.
No files were modified, no git commands were run, and no new locators were guessed or created — this is a plain read of the four existing files.