# Implementation Log 1 — Select Panelist Response Count / More Options

Branch: `investigation/data-test-id-locators`
File changed: [libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html](../../libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html)

Applied exactly the two changes proposed in
[data-test-id-phase2-response-count.md](data-test-id-phase2-response-count.md).
No other attribute, binding, class, or logic was touched.

## 1. Before/after diff

```diff
diff --git a/libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html b/libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html
index 8a5184f..84afa2b 100644
--- a/libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html
+++ b/libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html
@@ -33,11 +33,11 @@
 
           <div class="responses">
             @for (count of filteredResponseCounts; track count) {
-            <button [class.selected]="selectedResponses === count" (click)="selectResponse(count)">{{ count }}
+            <button [class.selected]="selectedResponses === count" (click)="selectResponse(count)" [attr.data-test-id]="'select-response-count-' + count">{{ count }}
               Responses</button>
             }
             @if (surveyCommonService.flowType().scratch) {
-            <button class="more-options" type="button" (click)="toggleMoreResponseOptions()">
+            <button class="more-options" type="button" data-test-id="toggle-more-response-options" (click)="toggleMoreResponseOptions()">
               <span aria-hidden="true">{{ showMoreResponseOptions ? '−' : '+' }}</span>
               {{ showMoreResponseOptions ? 'Fewer options' : 'More options' }}
             </button>
```

### Change 1 — response count button

**Before:**
```html
            <button [class.selected]="selectedResponses === count" (click)="selectResponse(count)">{{ count }}
              Responses</button>
```

**After:**
```html
            <button [class.selected]="selectedResponses === count" (click)="selectResponse(count)" [attr.data-test-id]="'select-response-count-' + count">{{ count }}
              Responses</button>
```

### Change 2 — "+ More options" button

**Before:**
```html
            <button class="more-options" type="button" (click)="toggleMoreResponseOptions()">
```

**After:**
```html
            <button class="more-options" type="button" data-test-id="toggle-more-response-options" (click)="toggleMoreResponseOptions()">
```

## 2. Local run / compile confirmation

**Not possible to fully confirm in this environment** — reporting honestly
rather than assuming success:

- Root `node_modules` was not installed in this workspace. Attempting
  `npm ci` failed with an `ERESOLVE` peer-dependency conflict
  (`@ngneat/helipopper` vs `@ngneat/overview`, pre-existing and unrelated to
  this change).
- Retrying with `npm install --legacy-peer-deps` got further, but failed
  partway through with `npm error code E401 Incorrect or missing password`
  while fetching private `@nielsen/*`-scoped packages from the internal
  Artifactory registry — this environment has no valid registry credentials
  configured, so a full dependency install cannot complete here.
- As a result, `@angular/cli` (`ng`) and `@angular/core` were never
  installed, and `npm run build:lib` / `ng serve` could not be executed
  (`'ng' is not recognized as an internal or external command`).

**What was verified instead:**
- The file's error/diagnostics check (via the editor's language
  diagnostics) reports **no errors** for
  `select-panelist.component.html` or its companion
  `select-panelist.component.ts` after the edit.
- The two added bindings use syntax already established elsewhere in this
  same file/codebase: `[attr.data-test-id]="'...' + expr"` for a
  dynamically-repeated element, and a plain static `data-test-id="..."`
  attribute (matching the existing `data-test-id="proceed-to-survey"` on the
  "next" button a few lines below in this same file) — no new binding
  pattern was introduced.
- No other attribute, class, or `(click)` binding on either button was
  changed; the diff above is the complete set of changes.

**Action needed from the user/CI:** an actual `ng serve` / `npm run
build:lib` run against a machine with valid Artifactory credentials is
still required to get a real runtime confirmation that the Select Panelists
screen renders and the response buttons / "More options" toggle still work.
That could not be performed from this sandboxed session.

## 3. Branch and file summary

| Item | Value |
|---|---|
| Branch | `investigation/data-test-id-locators` |
| File changed | `libs/survey-mod-lib/src/lib/features/select-panelist/components/select-panelist.component.html` |
| Elements changed | Response count button (dynamic `data-test-id`), "+ More options" button (static `data-test-id`) |
| Other files modified | This log file only |
