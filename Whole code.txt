PART 1 — Bring searchAndSelectDataset() (start-with-prompt.page.ts) up to
the same fixed pattern already proven in start-from-scratch.page.ts's
version — same fallback checks, adapted to this file's own resource names
(datasetTypeSelector, nextDatasetButton, datasetSearchToggle,
datasetSearchInput, datasetByText, applyDatasetButton — do NOT rename these
or assume they match start-from-scratch's names 1:1).

IMPORTANT — before implementing the above, first check whether an
equivalent to defaultDataset ([data-test-id='rv-default-dataset']) and
chooseDatasetBreadcrumb ([data-test-id='dataset-picker-title']) exists on
THIS flow's dataset screens — do not assume they do just because
btn-next-dataset and apply-dataset-btn are shared between both flows. If
you cannot confirm these elements exist (via existing captured DOM/test-
results, or by reasoning from the shared component structure), STOP here
and report exactly that — do not invent a locator or guess to make Part 1
"work," and do not proceed to Parts 2-4. We would need one live DOM check
before continuing, the same way rv-default-dataset itself was originally
confirmed.

If confirmed to exist, implement:
1. Check-before-select: if a dataset is already applied/showing, skip the
   type-selector/search flow entirely (mirror the defaultDataset +
   already-visible logic from the fixed start-from-scratch version, using
   whatever equivalent locators actually exist here).
2. Change the return type from Promise<void> to Promise<boolean>, following
   the same lesson learned today: only re-verify with an independent check
   when a real action (click) just happened — never re-check using the
   exact condition that already triggered a branch (e.g. do not call a
   breadcrumb-visibility check inside the branch that fired because the
   breadcrumb was already gone).

Show the diff for this part first before continuing to Part 2.

---

PART 2 — After applyDataset() succeeds, add the dimension/response-count
steps by reusing SelectPanelistPage DIRECTLY (import and instantiate it,
same as start-from-scratch.spec.ts does) — do NOT reimplement filter logic
here, it already has today's fallback fixes built in. Call
setDimensionSelections(datasetConfig.dimensionSelections), then
clickSearchForPanelists(), then click250Responses() — same individual-call
pattern as start-from-scratch, not the bundled panelist-flow.helper.ts,
since Part 3 needs to insert a step before the final button click.

---

PART 3 — Add the two genuinely new pieces:

1. New locator: { "elementName": "promptTextarea", "selectorType": "css",
   "selectorValue": "textarea.prompt-input" } (confirmed real, no
   data-test-id exists on this element per prior investigation of the
   library source).

2. New locator: { "elementName": "generateSurveyButton", "selectorType":
   "css", "selectorValue": "[data-test-id='proceed-to-survey']" } (confirmed
   real from library source — note this is the SAME button element used by
   the scratch flow's "Proceed to survey builder", just showing different
   text/disabled-condition here).

3. New method fillPromptAndGenerate(promptText: string): enter promptText
   into promptTextarea, then wait for generateSurveyButton to become
   enabled before clicking — do NOT assume it's clickable immediately after
   typing. The button's real disabled condition (confirmed from library
   source) is: !selectedResponses || (!userPrompt.trim() ||
   isPromptSettling()) — the isPromptSettling() name implies a debounce
   after typing. Use a real wait-for-enabled-state check if the framework
   supports one; otherwise use UI_STATE_UPDATE_DELAY as a minimum,
   explicitly commented as a fallback assumption, not a confirmed-correct
   wait.

---

PART 4 — Update start-with-prompt.spec.ts to chain all of the above:
navigate -> click Start with a prompt -> select dataset type -> search and
select dataset (assert the new boolean return from Part 1) -> apply dataset
-> dimension selections via SelectPanelistPage -> search for panelists ->
click250Responses() -> fillPromptAndGenerate() with sample text (e.g.
"Generate a few questions about product satisfaction") -> assert the flow
reached the next screen. For that final assertion, first check what a real,
existing indicator of "next screen reached" would be (e.g. a question-
generation or Build Survey page element) — do not guess a locator, report
if nothing confirmed is available yet.

Use datasetConfig.dimensionSelections and datasetConfig.searchTerm from
dev-test-data.json (same source start-from-scratch already uses) — do not
invent new test data.

---

For all 4 parts: run npx tsc --noEmit after each part and show output
before moving to the next part. Do NOT run the Playwright test until all 4
parts compile clean together.

If Part 1's DOM check fails to confirm the needed elements exist (see
IMPORTANT note above), stop immediately after Part 1 and report that —
do not proceed to Parts 2-4 in that case.

Once everything compiles and the full test has been run, create
e2e/START_WITH_PROMPT_IMPLEMENTATION.md documenting every change made
across all 4 parts (or the stop-point if Part 1 didn't confirm), the
diffs, the tsc output, and the actual test run output — same structure as
DATASET_SELECTION_BOOLEAN_FIX.md.