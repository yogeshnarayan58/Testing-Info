Investigated the actual code (no files touched yet — this is the proposed diff only, per your request to review first).

What I verified vs. guessed
Locator	Status
ds-addButton	✅ Confirmed real, but only found in omnishopper's DataSelector.clickAddIcon(), used there for adding a second+ folder while one is already selected. This repo's setDimensionSelection() currently opens the picker by clicking the dimension's own tile (ds-noDataPrompt_X), not an add button — there is no evidence this repo's DOM even has/needs ds-addButton for a first-time selection. I did not wire it in to avoid guessing at a UI path that could break the currently-passing flow. Flagging this as a deviation from your literal step 3/4 instructions.
ds-removeIcon-icon	✅ Confirmed real — found verbatim in omnishopper's data-selector-resources.json (removeIcon). Safe to reuse.
[data-cy='Selected-option']	❌ Guessed — not found anywhere in either repo.
ds-selectionMenu_Delete-icon	❌ Guessed — not found anywhere in either repo.
ds-checkbox-label-${text} (your suggested pattern)	❌ Does not match either repo. This repo's real checkboxLabel is div.item-container:has-text('${text}') niq-checkbox >> nth=0 — I reused the existing one, not your guessed pattern.
Placeholder text ("Select Product" etc.)	⚠️ Partially confirmed — a captured i18n bundle in test-results has "Select product" / "Select period" (lowercase, no Retailer/Geography/Demographic keys found). I used a generic /^select\b/i heuristic, not exact strings.
Reading "currently displayed value"	⚠️ Assumption: reusing the same ${dim}NoDataPrompt tile element (via .getText(), mirroring omnishopper's fetchMadlibDimensionValue()) for both empty and selected states — justified because the existing code already clicks this same element unconditionally regardless of state, implying it's a persistent single trigger/display tile, not a "no-data-only" element. Not confirmed against a live DOM (no browser access in this sandbox).
checkSelection/setMadlibFolder/enterTextInSearchField mechanics	✅ Cross-checked against omnishopper's data-selector.ts — confirms no isChecked/isSelected() exists there, matching your note; this repo's existing add/search flow is functionally equivalent and untouched.
Proposed diff
1. select-panelist.locator.json — add 3 resources after checkboxLabel:
     {
       "elementName": "checkboxLabel",
       "selectorType": "playwrights",
       "selectorValue": "div.item-container:has-text('${text}') niq-checkbox >> nth=0"
     },
+    {
+      "elementName": "removeIcon",
+      "selectorType": "tag",
+      "selectorValue": "[data-test-id='ds-removeIcon-icon']"
+    },
+    {
+      "elementName": "selectedOption",
+      "selectorType": "tag",
+      "selectorValue": "[data-cy='Selected-option']"
+    },
+    {
+      "elementName": "deleteMenuIcon",
+      "selectorType": "tag",
+      "selectorValue": "[data-test-id='ds-selectionMenu_Delete-icon']"
+    },
     {
       "elementName": "categoryItem",

2. select-panelist.page.ts — new private helpers after enterTextInSearchField() (~line 64), plus a check-before-click block inserted at the top of setDimensionSelection() (~line 87, right before the existing "Step 1: Click dimension prompt"):
   private async enterTextInSearchField(inputText: string): Promise<void> {
     const searchField = this.webActionHandler.getWebElement(
       this.resourceManager.getResource('dsSearchBarInput', SELECT_PANELIST_PAGE_COMPONENT_NAME)
     );
     await searchField.waitFor();
     await searchField.enterText("");
     await searchField.enterText(inputText);
   }
+
+  /**
+   * Reads the value currently displayed on a dimension's tile (same element
+   * setDimensionSelection() clicks to open the picker) — placeholder text like
+   * "Select Product" when empty, or the selected value once something's chosen.
+   */
+  private async getCurrentDimensionValue(dimensionPromptResource: string): Promise<string> {
+    const dimensionElement = this.webActionHandler.getWebElement(
+      this.resourceManager.getResource(dimensionPromptResource, SELECT_PANELIST_PAGE_COMPONENT_NAME)
+    );
+    await dimensionElement.waitFor();
+    const text = await dimensionElement.getText();
+    return (text ?? '').trim();
+  }
+
+  /**
+   * Best-effort placeholder heuristic (NOT a confirmed exact string per dimension —
+   * only "Select product"/"Select period" were found, lowercase, in captured i18n data).
+   */
+  private isPlaceholderValue(value: string): boolean {
+    return value.length === 0 || /^select\b/i.test(value.trim());
+  }
+
+  /** True if every desired item is already reflected in the current displayed text. */
+  private dimensionAlreadyMatches(currentValue: string, itemsToSelect: string[]): boolean {
+    if (this.isPlaceholderValue(currentValue) || itemsToSelect.length === 0) {
+      return false;
+    }
+    const normalizedCurrent = currentValue.toLowerCase();
+    return itemsToSelect.every((item) => normalizedCurrent.includes(item.toLowerCase()));
+  }
+
+  /**
+   * Removes the current selection before re-selecting. Direct remove-icon click first;
+   * falls back to hover + delete-menu-icon. `selectedOption`/`deleteMenuIcon` locators
+   * are UNVERIFIED — flagged in PR description, not found in either codebase.
+   */
+  private async removeCurrentDimensionSelection(): Promise<void> {
+    const removeIcon = this.webActionHandler.getWebElement(
+      this.resourceManager.getResource('removeIcon', SELECT_PANELIST_PAGE_COMPONENT_NAME)
+    );
+    try {
+      if (await removeIcon.isVisible()) {
+        await removeIcon.click();
+        await this.webActionHandler.delay(UI_STATE_UPDATE_DELAY);
+        return;
+      }
+    } catch {
+      // fall through to hover + delete-menu fallback
+    }
+
+    const selectedOption = this.webActionHandler.getWebElement(
+      this.resourceManager.getResource('selectedOption', SELECT_PANELIST_PAGE_COMPONENT_NAME)
+    );
+    await selectedOption.waitFor();
+    await selectedOption.hover();
+
+    const deleteMenuIcon = this.webActionHandler.getWebElement(
+      this.resourceManager.getResource('deleteMenuIcon', SELECT_PANELIST_PAGE_COMPONENT_NAME)
+    );
+    await deleteMenuIcon.waitFor();
+    await deleteMenuIcon.click();
+    await this.webActionHandler.delay(UI_STATE_UPDATE_DELAY);
+  }

   ): Promise<void> {
     try {
+      // Step 0: Check-before-click — skip if already correct, remove first if a
+      // different value is selected, otherwise fall through to the existing add flow.
+      const currentValue = await this.getCurrentDimensionValue(dimensionPromptResource);
+      if (this.dimensionAlreadyMatches(currentValue, itemsToSelect)) {
+        console.log(`↷ Dimension '${dimensionPromptResource}' already set to '${currentValue}' — skipping`);
+        return;
+      }
+      if (!this.isPlaceholderValue(currentValue)) {
+        console.log(`✎ Dimension '${dimensionPromptResource}' has a different value ('${currentValue}') — removing first`);
+        await this.removeCurrentDimensionSelection();
+      }
+
       // Step 1: Click dimension prompt
       const dimensionPrompt = this.webActionHandler.getWebElement(

Everything after that (steps 1–7: click tile, search icon, parent items, child items, Done) is unchanged.

Want me to apply this as-is, or adjust the ds-addButton gap first (e.g. confirm with you whether this repo's remove flow should re-click the tile vs. some add button before I finalize)?