# Investigation Only: OmniShopper Path Security, LambdaTest `.lt-sessions` and Cycode SAST

## Objective

This is an INVESTIGATION ONLY.

DO NOT modify any source files.
DO NOT refactor code.
DO NOT add or remove Cycode suppressions.
DO NOT change Cycode configuration.
DO NOT change CI/CD configuration.
DO NOT change dependencies.
DO NOT create a PR.
DO NOT implement a proposed fix.

At the END of the investigation, create ONLY this report:

`docs/investigation/omnishopper-cycode-path-security.md`

The purpose of this investigation is to understand how the OmniShopper repository handles filesystem paths, especially LambdaTest `.lt-sessions`, and whether any existing implementation provides useful evidence for a separate TypeScript/Playwright repository that currently receives Cycode SAST findings:

> Unsanitized dynamic input in file path

Our current repository already contains stronger path validation than some of the OmniShopper code we have seen. Therefore, DO NOT assume OmniShopper is more secure or that its implementation should be copied.

We are looking for useful structural/security patterns and evidence only.

---

# 1. Search All Filesystem and Path Handling

Search the entire repository for:

### Node path APIs

- `path.resolve`
- `path.join`
- `path.normalize`
- `path.relative`
- `path.isAbsolute`
- `path.basename`
- `path.dirname`

### Node filesystem APIs

- `fs.realpathSync`
- `fs.realpath`
- `fs.existsSync`
- `fs.readFileSync`
- `fs.writeFileSync`
- `fs.appendFileSync`
- `fs.mkdirSync`
- `fs.unlinkSync`
- `fs.rmSync`
- `fs.cpSync`
- `fs.renameSync`
- `createWriteStream`
- `createReadStream`

### Security/helper concepts

Search for helpers or equivalent implementations involving:

- `safePath`
- `safeJoin`
- `sanitizePath`
- `sanitizeFilePath`
- `validatePath`
- `validateFilePath`
- `securePath`
- `resolvePath`
- `containedPath`
- `assertContained`
- `isPathSafe`
- `isWithin`
- `withinDirectory`
- `allowedDirectory`
- `baseDirectory`
- `rootDirectory`

Do not rely only on helper names. Identify equivalent logic even if it uses a different name.

---

# 2. Trace Dynamic Input to Filesystem Sinks

For relevant examples, trace the complete data flow:

```text
Dynamic value
    ↓
Validation / sanitization
    ↓
Path construction
    ↓
Normalization / resolution
    ↓
Containment / security check
    ↓
Filesystem operationx