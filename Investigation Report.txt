# Investigation: `startWithScratch` not found in `startFromScratchPage` object repository

Investigation only — no files modified, no git commands run. All content below is read directly
from the current state of the files on disk.

## 1. `e2e/locators/survey/start-from-scratch.locator.json` — full current content

```json
{
  "name": "Survey Generation Page",
  "webElements": [
    { "elementName": "startFromScratch", "selectorType": "text", "selectorValue": "Start from scratch" },
    { "elementName": "productOfferingHeading", "selectorType": "playwrights", "selectorValue": "role=heading[name=\"Product Offering\"]" },
    { "elementName": "nextButton", "selectorType": "css", "selectorValue": "[data-test-id='btn-next-dataset']" },
    { "elementName": "searchToggle", "selectorType": "css", "selectorValue": "[data-test-id='dataset-picker-search-bar-toggle-search-button-icon']" },
    { "elementName": "searchInput", "selectorType": "css", "selectorValue": "[data-test-id='dataset-picker-search-bar-input']" },
    { "elementName": "datasetByLabel", "selectorType": "tag", "selectorValue": "[data-test-id^='rv-dataset-id-selector']:has-text('${text}')" },
    { "elementName": "firstDataset", "selectorType": "css", "selectorValue": "[data-test-id^='rv-dataset-id-selector']" },
    { "elementName": "entireDatasetOption", "selectorType": "tag", "selectorValue": "[data-test-id='rv-default-dataview']" },
    { "elementName": "dialogElement", "selectorType": "css", "selectorValue": "[data-test-id='title-second-dialog']" },
    { "elementName": "applyButton", "selectorType": "css", "selectorValue": "[data-test-id='apply-dataset-btn']" },
    { "elementName": "selectPanelistsHeading", "selectorType": "text", "selectorValue": "Select Panelists" },
    { "elementName": "defaultDataset", "selectorType": "css", "selectorValue": "[data-test-id='rv-default-dataset']" },
    { "elementName": "chooseDatasetBreadcrumb", "selectorType": "css", "selectorValue": "[data-test-id='dataset-picker-title']" },
    { "elementName": "chooseDataviewBreadcrumb", "selectorType": "css", "selectorValue": "[data-test-id='breadcrumb-and-search-container']" }
  ]
}
```

**Does a `startWithScratch` entry exist?** No.

Only a **`startFromScratch`** entry exists (note: `From`, not `With`):

```json
{ "elementName": "startFromScratch", "selectorType": "text", "selectorValue": "Start from scratch" }
```

There is no `startWithScratch` key anywhere in this file.

## 2. `StartFromScratchPage` constructor and `isStartFromScratchVisible()` — current content

From `e2e/page-objects/survey/start-from-scratch.page.ts`:

```typescript
const START_FROM_SCRATCH_PAGE_COMPONENT_NAME = 'startFromScratchPage';

export class StartFromScratchPage extends CommonPage {
  constructor(basePage: BasePage) {
    super(basePage);
    this.resourceManager.registerResources(
      START_FROM_SCRATCH_PAGE_COMPONENT_NAME,
      startFromScratchPageResources
    );
  }

  async clickStartFromScratch(): Promise<void> {
    try {
      const startFromScratch = this.webActionHandler.getWebElement(
        this.resourceManager.getResource('startWithScratch', START_FROM_SCRATCH_PAGE_COMPONENT_NAME)
      );
      await startFromScratch.waitFor(SMALL_TIMEOUT);
      await startFromScratch.click();
    } catch (error) {
      throw new Error(`Failed to click Start from scratch: ${error instanceof Error ? error.message : error}`);
    }
  }

  async isStartFromScratchVisible(): Promise<boolean> {
    const startFromScratch = this.webActionHandler.getWebElement(
      this.resourceManager.getResource('startWithScratch', START_FROM_SCRATCH_PAGE_COMPONENT_NAME)
    );
    await startFromScratch.waitFor(NAVIGATION_TIMEOUT);
    return await startFromScratch.isVisible();
  }
  ...
```

**Exact resource-name string passed to `getResource(...)` in `isStartFromScratchVisible()`:**

```
'startWithScratch'
```

**Does it match the `elementName` from step 1 character-for-character?**

**No.** The locator JSON defines `elementName: "startFromScratch"`, but both
`isStartFromScratchVisible()` and `clickStartFromScratch()` call
`getResource('startWithScratch', ...)`.

Difference: `startFromScratch` (locator file) vs. `startWithScratch` (page-object calls) —
`From` vs. `With`. This is a literal string mismatch, not a case or whitespace issue. This is the
exact cause the reported error message points to: `'startWithScratch' not found in the
startFromScratchPage object repository`.

## 3. `registerResources(...)` call and locator import — confirmation

Import statement (top of `start-from-scratch.page.ts`):

```typescript
import startFromScratchPageResources from '../../locators/survey/start-from-scratch.locator.json';
```

Constructor call:

```typescript
constructor(basePage: BasePage) {
  super(basePage);
  this.resourceManager.registerResources(
    START_FROM_SCRATCH_PAGE_COMPONENT_NAME,
    startFromScratchPageResources
  );
}
```

`registerResources(...)` is still called in the constructor, with the correct locator file
(`start-from-scratch.locator.json`) imported and registered under component name
`'startFromScratchPage'`. This part is correct — the registration itself is not the problem; the
problem is purely the resource-key string used later in `getResource('startWithScratch', ...)`.

## 4. `e2e/page-objects/common/dataset-selection.helper.ts` — full current content

```typescript
import { WebActions } from '@gfk/gta';
import { ResourceManager } from '../../utils/resource-manager';
import { SMALL_TIMEOUT, UI_STATE_UPDATE_DELAY } from '../../constants/timeout-constants';

/**
 * Resource key names for the dataset elements referenced by
 * searchAndSelectDataset(). start-from-scratch.page.ts and
 * start-with-prompt.page.ts register these under different elementName
 * values, so the caller supplies its own keys.
 */
export interface DatasetSelectionResourceKeys {
  datasetResourceKey: string;
  searchToggleResourceKey: string;
  searchInputResourceKey: string;
}

/**
 * Searches for and selects a dataset on the "Choose a dataset" screen.
 * Extracted from the near-identical searchAndSelectDataset() logic that was
 * duplicated in start-from-scratch.page.ts and start-with-prompt.page.ts:
 * check chooseDatasetBreadcrumb visibility -> fallback to
 * chooseDataviewBreadcrumb -> check default dataset text -> check
 * already-visible dataset element -> toggle search -> type search term ->
 * click dataset element.
 */
export async function searchAndSelectDataset(
  webActionHandler: WebActions,
  resourceManager: ResourceManager,
  componentName: string,
  searchTerm: string,
  datasetLabel: string,
  resourceKeys: DatasetSelectionResourceKeys
): Promise<boolean> {
  const { datasetResourceKey, searchToggleResourceKey, searchInputResourceKey } = resourceKeys;

  const chooseDatasetBreadcrumb = webActionHandler.getWebElement(
    resourceManager.getResource('chooseDatasetBreadcrumb', componentName)
  );
  const isOnDatasetScreen = await chooseDatasetBreadcrumb.isVisible();
  if (!isOnDatasetScreen) {
    const chooseDataviewBreadcrumb = webActionHandler.getWebElement(
      resourceManager.getResource('chooseDataviewBreadcrumb', componentName)
    );
    const hasReachedDataviewScreen = await chooseDataviewBreadcrumb.isVisible().catch(() => false);
    return hasReachedDataviewScreen;
  }

  const defaultDatasetElement = webActionHandler.getWebElement(
    resourceManager.getResource('defaultDataset', componentName)
  );
  const defaultDatasetText = await defaultDatasetElement.getText().catch(() => '');
  const isDefaultDatasetAlreadySelected = defaultDatasetText.trim().toLowerCase().includes(datasetLabel.trim().toLowerCase());
  if (isDefaultDatasetAlreadySelected) {
    return true;
  }

  const existingDatasetElement = webActionHandler.getWebElement(
    resourceManager.getDynamicResource(datasetResourceKey, datasetLabel, componentName)
  );
  const isDatasetAlreadyVisible = await existingDatasetElement.isVisible().catch(() => false);
  if (isDatasetAlreadyVisible) {
    await existingDatasetElement.click();
    return true;
  }

  const searchToggle = webActionHandler.getWebElement(
    resourceManager.getResource(searchToggleResourceKey, componentName)
  );
  await searchToggle.waitFor(SMALL_TIMEOUT);
  await searchToggle.click();

  const searchInput = webActionHandler.getWebElement(
    resourceManager.getResource(searchInputResourceKey, componentName)
  );
  await searchInput.waitFor(SMALL_TIMEOUT);
  await searchInput.enterText(searchTerm);
  await webActionHandler.delay(UI_STATE_UPDATE_DELAY);

  const datasetElement = webActionHandler.getWebElement(
    resourceManager.getDynamicResource(datasetResourceKey, datasetLabel, componentName)
  );
  await datasetElement.waitFor(SMALL_TIMEOUT);
  await datasetElement.click();
  return true;
}
```

**Does it independently import or register ANY locator resources itself?**

**No.** This helper file has no `import ... from '.../*.locator.json'` and never calls
`resourceManager.registerResources(...)`. It only calls `resourceManager.getResource(...)` and
`resourceManager.getDynamicResource(...)` using a `componentName` string and resource key strings
that are passed in as parameters by the caller (`StartFromScratchPage` passes
`START_FROM_SCRATCH_PAGE_COMPONENT_NAME` = `'startFromScratchPage'`).

It cannot be shadowing or overwriting the `startFromScratchPage` registration, because it performs
no registration of its own — it is purely a consumer of whatever was already registered by the
caller's constructor.

## 5. `e2e/page-objects/common/common-page.ts` — full current content

```typescript
import { BasePage, WebActions } from '@gfk/gta';
import { ResourceManager } from '../../utils/resource-manager';
import { NAVIGATION_TIMEOUT } from '../../constants/timeout-constants';
import { envConfig } from '../../config/env.config';

export class CommonPage {
  protected webActionHandler: WebActions;
  protected resourceManager: ResourceManager;

  constructor(protected basePage: BasePage) {
    this.webActionHandler = basePage.getWebActions();
    this.resourceManager = new ResourceManager({});
  }

  public async navigateToUrl(targetUrl: string, timeout: number = NAVIGATION_TIMEOUT): Promise<void> {
    try {
      await this.webActionHandler.navigateTo(targetUrl, timeout, 'domcontentloaded');
    } catch (navigationError) {
      console.error(`Navigation to URL failed: ${targetUrl}`, navigationError);
      throw navigationError;
    }
  }

  public buildUrl(baseURL: string, relativePath: string): string {
    const normalizedBaseURL = baseURL.endsWith('/') ? baseURL : `${baseURL}/`;
    return new URL(relativePath, normalizedBaseURL).toString();
  }

  public async navigateToSurvey(): Promise<void> {
    const surveyURL = this.buildUrl(envConfig.BASE_URL, 'survey');
    await this.navigateToUrl(surveyURL);
  }

  public getBasePage(): BasePage {
    return this.basePage;
  }
}
```

**Constructor / resourceManager setup:** `CommonPage`'s constructor runs first (via `super(basePage)`
in `StartFromScratchPage`'s constructor) and does exactly one thing to `resourceManager`:
`this.resourceManager = new ResourceManager({})` — creating a fresh, empty instance.

**Confirmed:** `CommonPage` never touches `resourceManager` again after construction — no
`registerResources`, no reset/clear method, no reassignment anywhere else in this file. The
subclass (`StartFromScratchPage`) constructor runs its own `super(basePage)` first (allocating the
empty `ResourceManager`), and only *afterward* calls
`this.resourceManager.registerResources(START_FROM_SCRATCH_PAGE_COMPONENT_NAME, startFromScratchPageResources)`.
That ordering is correct and nothing in `CommonPage` resets it afterward.

## Summary of findings

| # | Question | Finding |
|---|---|---|
| 1 | `startWithScratch` entry in locator JSON? | **Missing.** Only `startFromScratch` exists. |
| 2 | Resource key used by `isStartFromScratchVisible()` | `'startWithScratch'` — does **not** match `elementName: "startFromScratch"` (`From` vs `With`). |
| 3 | `registerResources(...)` present with correct import? | Yes — correct import and correct call, component name `'startFromScratchPage'`. |
| 4 | Does `dataset-selection.helper.ts` register its own resources? | No — it registers nothing; it only reads via keys/component name supplied by the caller. |
| 5 | Does `CommonPage` reset `resourceManager` after subclass registration? | No — it only initializes `resourceManager` once, before the subclass constructor calls `registerResources(...)`. |

The root cause of the reported error is isolated to item **#2**: `clickStartFromScratch()` and
`isStartFromScratchVisible()` in `start-from-scratch.page.ts` call
`getResource('startWithScratch', ...)`, a key that was never registered — the locator file only
defines `startFromScratch`. The "Fix-locator" commit did not change this call site, so the mismatch
persists. No fix has been applied as part of this investigation, per instructions.
