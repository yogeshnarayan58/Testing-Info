# Dynamic Input Investigation — `e2e/playwright/` and `e2e/utils/`

Investigation-only pass. No files were modified as part of producing this document (other
than creating this document itself). Every dynamic value that reaches a file-path
construction, a `console.log`/`warn`/`error` statement, or an equivalent sensitive sink
(URL construction, `fetch()` call, file content write) in the two target folders is
traced back to its source and its current sanitization status is recorded — whether or
not sanitization is already present.

## Files examined

Only 5 files exist under `e2e/playwright/` (incl. `reporters/`, `setup/`) and `e2e/utils/`
at the time of this investigation:

| # | File |
|---|---|
| 1 | `e2e/playwright/reporters/lt-session-reporter.ts` |
| 2 | `e2e/playwright/setup/globalTeardown.ts` |
| 3 | `e2e/utils/base-page-factory.ts` |
| 4 | `e2e/utils/configurationDetails.ts` |
| 5 | `e2e/utils/resource-manager.ts` |

No other files (interfaces, constants, etc.) exist directly under these two folders —
`resource-manager.ts` imports `../interfaces/iElementResource`, but that file lives
outside `e2e/utils/` (in `e2e/interfaces/`) and is therefore out of scope per the request.

---

## 1. `e2e/playwright/reporters/lt-session-reporter.ts` — full verbatim content

```typescript
/**
 * Custom Playwright reporter to capture LambdaTest session names.
 * NOTE: This reporter does NOT create sessions - it only captures session names created by
 * BasePageFactory (which uses getLambdaTestBuildName for consistent build naming).
 * Stores test -> session mapping into individual per-test JSON files
 * (test-results/.lt-sessions/test-<id>-<pid>-<timestamp>.json) to avoid concurrent write conflicts.
 *
 * This reporter is a complete no-op when LAMBDATEST !== 'true', so it has zero effect
 * on the default/local test run.
 */
import { Reporter, TestCase, TestResult, FullResult } from '@playwright/test/reporter';
import * as fs from 'fs';
import * as path from 'path';
import { getLambdaTestSessionDir } from '../../utils/configurationDetails';

class LTSessionReporter implements Reporter {
  private testSessions = new Map<string, string>(); // fullName -> sessionName

  onBegin() {
    if (process.env.LAMBDATEST !== 'true') return;

    console.log('[LT Reporter] Initialized - capturing LambdaTest sessions...');
  }

  onTestBegin(test: TestCase) {
    if (process.env.LAMBDATEST !== 'true') return;

    process.env.__CURRENT_TEST_ID__ = test.id;
    (global as any).__CURRENT_TEST_ID__ = test.id;

    console.log(`[LT Reporter] Test started: "${test.title}" (testId: ${test.id})`);
  }

  onTestEnd(test: TestCase, result: TestResult) {
    if (process.env.LAMBDATEST !== 'true') return;

    const testId = test.id;
    // Sanitize testId before using it in any file path (defense-in-depth; test.id is
    // already a fixed-format SHA1-hex pair, but we sanitize explicitly for SAST clarity).
    const safeTestId = testId.replace(/[^a-zA-Z0-9-]/g, '_');
    const sessionDir = getLambdaTestSessionDir();
    const testSessionFile = path.join(sessionDir, `test-${safeTestId}.json`);

    let sessionName: string | null = null;

    try {
      // 1st priority: test-specific session file
      if (fs.existsSync(testSessionFile)) {
        const data = JSON.parse(fs.readFileSync(testSessionFile, 'utf-8'));
        sessionName = data.sessionName;

        // cleanup
        try {
          fs.unlinkSync(testSessionFile);
        } catch {
          /* ignore cleanup failure */
        }
      }

      // fallback: sessions.jsonl
      if (!sessionName) {
        // Static filename, no dynamic input.
        const sessionsLogFile = path.join(sessionDir, 'sessions.jsonl');
        // Not used in a file path, only used for in-memory matching against sessions.jsonl entries.
        const workerIndex = result.parallelIndex ?? 0;
        const testStartTime = result.startTime?.getTime() || 0;

        if (fs.existsSync(sessionsLogFile)) {
          const lines = fs.readFileSync(sessionsLogFile, 'utf-8').split('\n').filter(Boolean);

          let bestMatch: { sessionName: string; diff: number } | null = null;

          for (const line of lines) {
            try {
              const s = JSON.parse(line);
              if (s.workerIndex === workerIndex && s.timestamp && testStartTime) {
                const diff = Math.abs(s.timestamp - testStartTime);
                if (diff < 60000 && (!bestMatch || diff < bestMatch.diff)) {
                  bestMatch = { sessionName: s.sessionName, diff };
                }
              }
            } catch {
              /* ignore malformed line */
            }
          }

          if (bestMatch) sessionName = bestMatch.sessionName;
        }
      }
    } catch (err) {
      console.error('[LT Reporter] Error reading session:', err);
    }

    if (!sessionName) {
      console.warn(`[LT Reporter] No session found for "${test.title}" (testId: ${testId})`);
      return;
    }

    const fullName = test.titlePath().filter(Boolean).join(' › ');
    // Normalize > separator to › for consistent mapping with globalTeardown
    const normalizedFullName = fullName.replace(/\s*>\s*/g, ' › ');

    this.testSessions.set(normalizedFullName, sessionName);

    console.log(`[LT Reporter] Stored: "${normalizedFullName}" -> ${sessionName}`);

    // Write mapping per test/worker to avoid concurrent write conflicts
    try {
      fs.mkdirSync(sessionDir, { recursive: true });

      // Use a collision-resistant filename per test/process to prevent concurrent write conflicts.
      // Reuses the safeTestId sanitized above (testId.replace(/[^a-zA-Z0-9-]/g, '_')).
      const mappingFile = path.join(sessionDir, `test-${safeTestId}-${process.pid}-${Date.now()}.json`);
      fs.writeFileSync(
        mappingFile,
        JSON.stringify(
          {
            testName: test.title,
            fullName: normalizedFullName,
            testId,
            sessionName,
            timestamp: Date.now(),
          },
          null,
          2
        ),
        'utf-8'
      );

      console.log(`[LT Reporter] Mapping written to ${path.basename(mappingFile)}`);
    } catch (err) {
      console.error('[LT Reporter] Failed to write mapping:', err);
    }
  }

  onEnd(result: FullResult) {
    if (process.env.LAMBDATEST !== 'true') return;

    if (this.testSessions.size === 0) {
      console.warn('[LT Reporter] No sessions captured.');
      return;
    }

    try {
      const sessionDir = getLambdaTestSessionDir();
      fs.mkdirSync(sessionDir, { recursive: true });

      // Static filename, no dynamic input.
      const mappingFile = path.join(sessionDir, 'session-mappings.json');
      fs.writeFileSync(mappingFile, JSON.stringify(Object.fromEntries(this.testSessions), null, 2));

      console.log(`[LT Reporter] Saved ${this.testSessions.size} session mappings.`);
    } catch (err) {
      console.error('[LT Reporter] Failed to save mappings:', err);
    }
  }
}

export default LTSessionReporter;
```

### Dynamic-value sinks in this file

**L31** —
```typescript
console.log(`[LT Reporter] Test started: "${test.title}" (testId: ${test.id})`);
```
- `test.title`: origin — Playwright `TestCase.title`, the developer-authored test name from the `.spec.ts` file's `test('...')` call. Sanitized: **None**.
- `test.id`: origin — Playwright `TestCase.id` (fixed `/^[0-9a-f]{20}-[0-9a-f]{20}$/` SHA1-hex pair, computed internally by Playwright from file+title, per `e2e/node_modules/playwright/lib/common/index.js`). Sanitized: **None** (format is inherently safe, but no explicit sanitizer at this line).

**L42** —
```typescript
const testSessionFile = path.join(sessionDir, `test-${safeTestId}.json`);
```
- `safeTestId`: origin — `test.id` (L37 `const testId = test.id;`). Sanitized: **Yes** — `const safeTestId = testId.replace(/[^a-zA-Z0-9-]/g, '_');` (L39).
- `sessionDir`: origin — `getLambdaTestSessionDir()` → `path.join(process.cwd(), 'test-results', '.lt-sessions')` (see `configurationDetails.ts`). Sanitized: N/A — built entirely from `process.cwd()` + literals, not from tainted/attacker-controllable input.

**L49–50** —
```typescript
const data = JSON.parse(fs.readFileSync(testSessionFile, 'utf-8'));
sessionName = data.sessionName;
```
- `data.sessionName`: origin — content of the JSON file written by `base-page-factory.ts` (`sessionName = \`Test-${timestamp}\`` there). This is a value **read from a file**. Sanitized: **None** at the point it's read; flows unsanitized into `sessionName`, which later reaches a log sink (see L105 below).

**L65** —
```typescript
const workerIndex = result.parallelIndex ?? 0;
```
- Origin — Playwright's `TestResult.parallelIndex` (engine-assigned numeric worker slot). Not used in a path or log in this file (only in the in-memory comparison at L75). Sanitized: **None applied**, but also not reaching a path/log sink here.

**L69–79** — `fs.readFileSync(sessionsLogFile, ...)` → per-line `JSON.parse(line)` → `s.workerIndex`, `s.timestamp`, `s.sessionName`.
- Origin — content of `sessions.jsonl`, written by `base-page-factory.ts` (`fs.appendFileSync(path.join(sessionDir, 'sessions.jsonl'), JSON.stringify({ sessionName, timestamp, workerIndex }) + '\n', ...)`). These are values **read from a file**. `bestMatch.sessionName` (from here) can become `sessionName`, which later reaches the log sink at L105. Sanitized: **None** at read time.

**L91** —
```typescript
console.error('[LT Reporter] Error reading session:', err);
```
- `err`: origin — exception thrown inside the surrounding `try` (e.g., malformed JSON from `testSessionFile`/`sessionsLogFile`). Sanitized: **None** (passed as-is to `console.error`; this is idiomatic Node error logging rather than a raw string interpolation, but is still an unsanitized dynamic value at a log sink).

**L95** —
```typescript
console.warn(`[LT Reporter] No session found for "${test.title}" (testId: ${testId})`);
```
- `test.title`: same origin as L31. Sanitized: **None**.
- `testId`: **raw** `test.id` (not `safeTestId`) is logged here. Sanitized: **None** at this line (the `safeTestId` sanitization from L39 is not applied to this particular log call — it only guards the earlier path-building line).

**L105** —
```typescript
console.log(`[LT Reporter] Stored: "${normalizedFullName}" -> ${sessionName}`);
```
- `normalizedFullName`: origin — `test.titlePath()` (L98, developer-authored suite/describe/test names) then regex-normalized at L100 (`fullName.replace(/\s*>\s*/g, ' › ')` — a formatting normalization, not a security sanitizer). Sanitized: **None** (security-wise).
- `sessionName`: origin — either the file-content read at L49–50 or the `sessions.jsonl` fallback at L69–79 (both ultimately traced to `base-page-factory.ts`'s internally generated `` `Test-${timestamp}` `` string). Sanitized: **None** explicitly at this log line.

**L113** —
```typescript
const mappingFile = path.join(sessionDir, `test-${safeTestId}-${process.pid}-${Date.now()}.json`);
```
- `safeTestId`: reused from L39. Sanitized: **Yes** (same regex as above).
- `process.pid`: origin — Node.js process ID, OS-assigned integer. Sanitized: **None applied**, but inherently numeric (not string/attacker-influenced).
- `Date.now()`: origin — runtime timestamp (integer). Sanitized: **None applied**, inherently numeric.

**L130** —
```typescript
console.log(`[LT Reporter] Mapping written to ${path.basename(mappingFile)}`);
```
- `mappingFile`: built at L113 (see above). Sanitized: **Yes** — wrapped in `path.basename(...)` directly at the log call.

**L132** —
```typescript
console.error('[LT Reporter] Failed to write mapping:', err);
```
- `err`: exception object from the `try` block (e.g., `fs.writeFileSync` failure). Sanitized: **None** (standard error-object logging).

**L149** —
```typescript
const mappingFile = path.join(sessionDir, 'session-mappings.json');
```
- No dynamic value — `'session-mappings.json'` is a literal. Already commented `// Static filename, no dynamic input.` Not a finding.

**L152** —
```typescript
console.log(`[LT Reporter] Saved ${this.testSessions.size} session mappings.`);
```
- `this.testSessions.size`: a `Map.size` (non-negative integer), not derived from external/tainted input. Sanitized: N/A (not a risk-relevant dynamic value).

**L154** —
```typescript
console.error('[LT Reporter] Failed to save mappings:', err);
```
- `err`: exception object. Sanitized: **None** (standard error-object logging).

---

## 2. `e2e/playwright/setup/globalTeardown.ts` — full verbatim content

```typescript
import * as fs from 'fs';
import * as path from 'path';
import { getLambdaTestBuildName, getLambdaTestSessionDir } from '../../utils/configurationDetails';
import { MAX_TEST_TIMEOUT } from '../../constants/timeout-constants';

const ALLURE_RESULTS_DIR = path.join(process.cwd(), 'allure-results');
// Static path: literal filename joined onto a hardcoded directory, no dynamic input.
const ENV_PROPS_PATH = path.join(ALLURE_RESULTS_DIR, 'environment.properties');

const sanitizeId = (value: unknown): string => String(value).replace(/[^a-zA-Z0-9_-]/g, '');

/**
 * globalTeardown.ts: Runs after all tests complete.
 *
 * When LAMBDATEST=true, this:
 *  - Reads the test-results/.lt-sessions/ test->session mapping files written by
 *    lt-session-reporter.ts / base-page-factory.ts,
 *  - Injects an LT_SESSION parameter into the matching Allure result files,
 *  - Calls the LambdaTest Automation API to fetch session info (video link,
 *    session id) and patch each session's pass/fail status,
 *  - Writes allure-results/environment.properties with the LambdaTest build URL.
 *
 * When LAMBDATEST is not 'true', this is a complete no-op so local/default
 * test runs are entirely unaffected.
 */
export default async () => {
  const isLambdaTest = process.env.LAMBDATEST === 'true';

  if (!isLambdaTest) {
    return;
  }

  try {
    console.log('[global-teardown] Adding LambdaTest videos to Allure report...');

    const ltUser = process.env.SM_LAMBDATEST_USERNAME || process.env.SM_LT_USERNAME;
    const ltAccessKey = process.env.SM_LAMBDATEST_ACCESS_KEY || process.env.SM_LT_ACCESS_KEY;
    const ltBuild = process.env.SM_LT_BUILD || 'Discover build';
    const buildIdentifier =
      process.env.SM_LT_BUILD_IDENTIFIER ||
      process.env.BUILD_NUMBER ||
      process.env.GITHUB_RUN_NUMBER ||
      undefined;

    // Construct the actual build name used in LambdaTest.
    // getLambdaTestBuildName centralizes the naming convention so that both
    // playwright.config.ts and this teardown step use the same format, ensuring
    // LambdaTest sessions are grouped correctly and can be reliably correlated.
    const actualBuildName = getLambdaTestBuildName(ltBuild, buildIdentifier);

    fs.mkdirSync(ALLURE_RESULTS_DIR, { recursive: true });

    console.log('[global-teardown] Step 1: Reading test-session mappings...');
    const sessionDir = getLambdaTestSessionDir();

    const testSessionMap = new Map<string, string>(); // fullName -> sessionName
    const testIdMap = new Map<string, string>(); // testId -> sessionName

    // Read individual mapping files (one per test) to avoid concurrent write conflicts
    if (fs.existsSync(sessionDir)) {
      const mappingFiles = fs
        .readdirSync(sessionDir)
        .filter((f) => f.startsWith('test-') && f.endsWith('.json'));

      console.log(`[global-teardown] Found ${mappingFiles.length} test-session mapping file(s)`);

      for (const file of mappingFiles) {
        // Sanitize the on-disk filename up-front so both the path built below and the log
        // statement in the catch block use the same sanitized value (never the raw entry).
        const sanitizedFile = path.basename(file);
        try {
          const filePath = path.join(sessionDir, sanitizedFile);
          const data = JSON.parse(fs.readFileSync(filePath, 'utf-8'));

          if (data.sessionName) {
            if (data.fullName) {
              testSessionMap.set(data.fullName.trim(), data.sessionName);
            }
            if (data.testId) {
              testIdMap.set(data.testId, data.sessionName);
            }
          }
        } catch (e) {
          console.warn(`[global-teardown] Failed to parse mapping file ${sanitizedFile}:`, e);
        }
      }
    } else {
      console.warn(`[global-teardown] No LambdaTest session directory found at ${sessionDir}`);
    }

    // Step 2: Read Allure result files
    console.log('[global-teardown] Step 2: Reading Allure result files...');
    const allureFiles = fs.existsSync(ALLURE_RESULTS_DIR)
      ? fs.readdirSync(ALLURE_RESULTS_DIR).filter((f) => f.endsWith('-result.json'))
      : [];

    console.log(`[global-teardown] Found ${allureFiles.length} Allure result files`);

    const testResults = allureFiles.map((file) => {
      // Sanitize filename to prevent path traversal attacks
      const sanitizedFile = path.basename(file);
      const filePath = path.join(ALLURE_RESULTS_DIR, sanitizedFile);
      const result = JSON.parse(fs.readFileSync(filePath, 'utf-8'));
      return { filePath, result };
    });

    // Step 3: Inject LT_SESSION into Allure result files
    console.log('[global-teardown] Step 3: Injecting LT_SESSION into Allure files...');
    for (const testResult of testResults) {
      const result = testResult.result;

      const testName = result.name?.trim();
      if (!testName) continue;

      const testId = result.testCaseId;

      const labels = result.labels || [];
      const parentSuite = labels.find((l: any) => l.name === 'parentSuite')?.value;
      const suite = labels.find((l: any) => l.name === 'suite')?.value;
      const subSuite = labels.find((l: any) => l.name === 'subSuite')?.value;

      const fullName = [parentSuite, suite, subSuite, testName].filter(Boolean).join(' › ').trim();

      // Normalize > separator to › for consistent mapping with reporter
      const normalizedFullName = fullName.replace(/\s*>\s*/g, ' › ');

      let sessionName = testSessionMap.get(normalizedFullName);

      if (!sessionName && testId) {
        sessionName = testIdMap.get(testId);
      }

      if (sessionName) {
        result.parameters = result.parameters || [];
        const hasLTSession = result.parameters.some((p: any) => p.name === 'LT_SESSION');

        if (!hasLTSession) {
          result.parameters.push({ name: 'LT_SESSION', value: sessionName });
          // Safe: testResult.filePath was sanitized with path.basename() during file reading
          fs.writeFileSync(testResult.filePath, JSON.stringify(result, null, 2));
          console.log(`[global-teardown]  Added LT_SESSION for "${fullName}": ${sessionName}`);
        }
      } else {
        console.warn(`[global-teardown]  No session mapping found for: "${fullName}"`);
      }
    }

    // Step 4: Fetch LambdaTest sessions from API
    console.log('[global-teardown] Step 4: Fetching LambdaTest sessions...');
    if (!ltUser || !ltAccessKey) {
      console.warn('[global-teardown] Missing LambdaTest credentials. Skipping API calls.');
      return;
    }

    const auth = Buffer.from(`${ltUser}:${ltAccessKey}`).toString('base64');

    const response = await fetch(
      `https://api.lambdatest.com/automation/api/v1/sessions?build=${encodeURIComponent(actualBuildName)}&limit=100`,
      {
        headers: { Authorization: `Basic ${auth}` },
        signal: AbortSignal.timeout(MAX_TEST_TIMEOUT),
      }
    );

    if (!response.ok) {
      const errorBody = await response.text().catch(() => '');
      throw new Error(
        `[global-teardown] LambdaTest API failed: ${response.status} ${response.statusText}` +
          (errorBody ? ` Body: ${errorBody}` : '')
      );
    }

    const data = await response.json();
    const sessions = data?.data || [];

    console.log(`[global-teardown] Found ${sessions.length} LambdaTest sessions`);

    // Extract build ID from the first session that exposes it
    let buildId: string | undefined;
    for (const session of sessions) {
      const sessionBuildId = session.build_id || session.buildId || session.build_number;
      if (sessionBuildId) {
        // Sanitize once at the source: buildId flows into a log line, a URL, and a static
        // properties file below, so sanitizing here covers all of those sinks.
        buildId = sanitizeId(sessionBuildId);
        console.log(`[global-teardown] Extracted Build ID: ${buildId}`);
        break;
      }
    }

    // Step 5: Add LambdaTest video links to Allure
    console.log('[global-teardown] Step 5: Adding video links to Allure files...');

    for (const testResult of testResults) {
      const result = testResult.result;
      const testName = result.name?.trim();

      if (!testName) continue;

      const ltSessionParam = result.parameters?.find((p: any) => p.name === 'LT_SESSION');
      const sessionName = ltSessionParam?.value;

      if (!sessionName) {
        console.warn(`[global-teardown] No LT_SESSION parameter for test: ${testName}`);
        continue;
      }

      console.log(`[global-teardown] Looking for session: ${sessionName}`);
      const session = sessions.find((s: any) => s.name === sessionName);
      if (!session) {
        // Session names come from the external LambdaTest API response; sanitize before logging.
        const safeAvailableNames = sessions
          .slice(0, 5)
          .map((s: any) => sanitizeId(s.name))
          .join(', ');
        console.warn(`[global-teardown] Session "${sessionName}" not found in LT API`);
        console.warn(`[global-teardown]    Available sessions (first 5): ${safeAvailableNames}`);
        continue;
      }
      // session_id comes from the external LambdaTest API response; sanitize once and reuse
      // for the log line, the video link URL, and the status-update URL below.
      const safeSessionId = sanitizeId(session.session_id);
      console.log(`[global-teardown] ✓ Found session in LT API: ${safeSessionId}`);

      result.links = result.links || [];
      result.links = result.links.filter((link: any) => link.name !== 'LambdaTest Video');

      result.links.push({
        name: 'LambdaTest Video',
        url: `https://automation.lambdatest.com/logs/?sessionID=${safeSessionId}`,
        type: 'link',
      });

      // Update LT status
      const testStatus = result.status === 'passed' ? 'passed' : 'failed';

      try {
        await fetch(`https://api.lambdatest.com/automation/api/v1/sessions/${safeSessionId}`, {
          method: 'PATCH',
          headers: {
            Authorization: `Basic ${auth}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ status_ind: testStatus }),
          signal: AbortSignal.timeout(MAX_TEST_TIMEOUT),
        });
      } catch (e) {
        console.warn(`[global-teardown] Failed to update status for session ${safeSessionId}`, e);
      }

      // Safe: testResult.filePath was sanitized with path.basename() during file reading
      fs.writeFileSync(testResult.filePath, JSON.stringify(result, null, 2));
      console.log(`[global-teardown] ✓ Added video link for "${testName}"`);
    }

    // Step 6: environment.properties write
    if (buildId) {
      process.env.LT_BUILD_ID = buildId;
    }

    // process.env.LT_BUILD_ID is only ever set from the already-sanitized `buildId` above
    // (a few lines up: `process.env.LT_BUILD_ID = buildId;`), but sanitize again defensively
    // in case the env var was pre-populated by an external caller.
    const ltBuildId = buildId || (process.env.LT_BUILD_ID ? sanitizeId(process.env.LT_BUILD_ID) : undefined);

    let buildUrl: string;

    if (ltBuildId) {
      // Use simple format: ?build=<id> (not pageType)
      buildUrl = `https://automation.lambdatest.com/build?build=${ltBuildId}`;
    } else {
      // Fallback to build name search
      buildUrl = `https://automation.lambdatest.com/build?buildName=${encodeURIComponent(actualBuildName)}`;
    }

    let envContent =
      `LambdaTest.Build.URL=${buildUrl}\n` +
      `LambdaTest.Build.Name=${actualBuildName}\n` +
      (buildIdentifier ? `LambdaTest.Build.Identifier=${buildIdentifier}\n` : '') +
      (ltBuildId ? `LambdaTest.Build.ID=${ltBuildId}\n` : '') +
      `Execution.Mode=LambdaTest Cloud\n`;

    // Safe: ENV_PROPS_PATH uses path.join() with hardcoded directory and filename, no user input
    fs.writeFileSync(ENV_PROPS_PATH, envContent, 'utf-8');

    console.log('[global-teardown] LambdaTest videos added to Allure report successfully.');
    console.log(`[global-teardown] Build URL: ${buildUrl}`);
  } catch (error) {
    console.warn('[global-teardown] Failed to add LambdaTest info:', error);
  }
};
```

### Dynamic-value sinks in this file

**L6** — `const ALLURE_RESULTS_DIR = path.join(process.cwd(), 'allure-results');`
- `process.cwd()`: origin — Node process working directory (OS-controlled, not attacker input). Sanitized: **None applied**, no comment present, but not a tainted-input path.

**L8** — `const ENV_PROPS_PATH = path.join(ALLURE_RESULTS_DIR, 'environment.properties');`
- No externally dynamic value — literal filename onto `ALLURE_RESULTS_DIR`. Already commented `// Static path...`. Not a finding.

**L36–37** — `const ltUser = process.env.SM_LAMBDATEST_USERNAME || process.env.SM_LT_USERNAME;` / `const ltAccessKey = ...`
- Origin: environment variables (credentials). Traced forward: only ever used at L155 (`Buffer.from(...).toString('base64')` for a Basic-auth header) and in the `!ltUser || !ltAccessKey` boolean check at L150 — **never logged or placed in a file path/URL directly** in this file. Sanitized: N/A (never reaches a log/path sink raw).

**L38–43** — `ltBuild`, `buildIdentifier` from `process.env.SM_LT_BUILD` / `SM_LT_BUILD_IDENTIFIER` / `BUILD_NUMBER` / `GITHUB_RUN_NUMBER`.
- Flow into `actualBuildName` (L49) via `getLambdaTestBuildName()`. See sinks below.

**L65** — `console.log(\`[global-teardown] Found ${mappingFiles.length} test-session mapping file(s)\`);`
- `mappingFiles.length`: a count (number), not tainted. Sanitized: N/A.

**L70 / L84** — 
```typescript
const sanitizedFile = path.basename(file);
try {
  const filePath = path.join(sessionDir, sanitizedFile);
  ...
} catch (e) {
  console.warn(`[global-teardown] Failed to parse mapping file ${sanitizedFile}:`, e);
}
```
- `file`: origin — `fs.readdirSync(sessionDir)` entries (on-disk filenames written by `lt-session-reporter.ts`/`base-page-factory.ts`). Sanitized: **Yes** — `path.basename(file)` (L70), reused for both the path (L72) and the log (L84).
- `e`: exception object. Sanitized: **None** (standard error-object logging).

**L88** — `console.warn(\`[global-teardown] No LambdaTest session directory found at ${sessionDir}\`);`
- `sessionDir`: origin — `getLambdaTestSessionDir()` (see `configurationDetails.ts`), built from `process.cwd()` + literals. Sanitized: **None applied**, not attacker-controllable.

**L97** — `console.log(\`[global-teardown] Found ${allureFiles.length} Allure result files\`);`
- `allureFiles.length`: count, not tainted. Sanitized: N/A.

**L101–102** —
```typescript
const sanitizedFile = path.basename(file);
const filePath = path.join(ALLURE_RESULTS_DIR, sanitizedFile);
```
- `file`: origin — `fs.readdirSync(ALLURE_RESULTS_DIR)` entries (Allure-generated result filenames). Sanitized: **Yes** — `path.basename(file)`.

**L141** — `console.log(\`[global-teardown]  Added LT_SESSION for "${fullName}": ${sessionName}\`);`
- `fullName`: origin — built at L120–124 from `result.name`, and `labels` (`parentSuite`/`suite`/`subSuite`), all read from the Allure result JSON file content (`JSON.parse(fs.readFileSync(filePath, ...))` at L103). A value **read from a file**. Sanitized: **None**.
- `sessionName`: origin — `testSessionMap`/`testIdMap`, populated from the per-test mapping-file content (L75–81), itself written by `lt-session-reporter.ts`/`base-page-factory.ts`. Sanitized: **None**.

**L144** — `console.warn(\`[global-teardown]  No session mapping found for: "${fullName}"\`);`
- `fullName`: same origin/sanitization as L141. Sanitized: **None**.

**L151** — `console.warn('[global-teardown] Missing LambdaTest credentials. Skipping API calls.');`
- No dynamic value (does not log `ltUser`/`ltAccessKey` — this is the good pattern, matching the `'configured'/'missing'` redaction style used in `playwright.config.ts`). Not a finding.

**L155** — `const auth = Buffer.from(\`${ltUser}:${ltAccessKey}\`).toString('base64');`
- `ltUser`/`ltAccessKey`: origin — env vars (L36–37). Sink: string template used to build a Basic-auth token (not a path or `console.*`, but a "similar sensitive sink" — credential material entering a string built via interpolation). Sanitized: **None** — this is inherent/necessary (it's the auth mechanism itself), not a place sanitization would apply without breaking behavior. The token itself (`auth`) is never logged.

**L158** — `` `https://api.lambdatest.com/automation/api/v1/sessions?build=${encodeURIComponent(actualBuildName)}&limit=100` ``
- `actualBuildName`: origin — `getLambdaTestBuildName(ltBuild, buildIdentifier)` (L49), built from `process.env.SM_LT_BUILD`/`SM_LT_BUILD_IDENTIFIER`/`BUILD_NUMBER`/`GITHUB_RUN_NUMBER`. Sink: URL used in a `fetch()` call. Sanitized: **Yes** — `encodeURIComponent(actualBuildName)`.

**L176** — `console.log(\`[global-teardown] Found ${sessions.length} LambdaTest sessions\`);`
- `sessions.length`: count from the LambdaTest API response array, not tainted. Sanitized: N/A.

**L181–186** —
```typescript
const sessionBuildId = session.build_id || session.buildId || session.build_number;
if (sessionBuildId) {
  buildId = sanitizeId(sessionBuildId);
  console.log(`[global-teardown] Extracted Build ID: ${buildId}`);
  break;
}
```
- `sessionBuildId`/`buildId`: origin — external LambdaTest Automation API JSON response (`session.build_id`/`buildId`/`build_number`). Sink: `console.log` here, plus downstream URL (L270) and file content (L280, via `ltBuildId`). Sanitized: **Yes** — `sanitizeId(sessionBuildId)` applied once at the point of extraction (L185), covering all downstream uses.

**L204** — `console.warn(\`[global-teardown] No LT_SESSION parameter for test: ${testName}\`);`
- `testName`: origin — `result.name?.trim()`, read from the Allure result JSON file content. Sanitized: **None**.

**L208** — `console.log(\`[global-teardown] Looking for session: ${sessionName}\`);`
- `sessionName`: origin — `result.parameters` `LT_SESSION` value, itself derived from the mapping-file content injected in Step 3 (ultimately from `base-page-factory.ts`'s generated session name). Sanitized: **None**.

**L214–217** —
```typescript
const safeAvailableNames = sessions
  .slice(0, 5)
  .map((s: any) => sanitizeId(s.name))
  .join(', ');
console.warn(`[global-teardown] Session "${sessionName}" not found in LT API`);
console.warn(`[global-teardown]    Available sessions (first 5): ${safeAvailableNames}`);
```
- `s.name` (mapped into `safeAvailableNames`): origin — external LambdaTest API response. Sink: `console.warn`. Sanitized: **Yes** — `sanitizeId(s.name)` per element.
- `sessionName` (in the first `console.warn`): same origin as L208. Sanitized: **None** at this line.

**L222–223** —
```typescript
const safeSessionId = sanitizeId(session.session_id);
console.log(`[global-teardown] ✓ Found session in LT API: ${safeSessionId}`);
```
- `session.session_id`: origin — external LambdaTest API response. Sink: `console.log` (and reused below for URL/log sinks). Sanitized: **Yes** — `sanitizeId(session.session_id)`.

**L233** — `` url: `https://automation.lambdatest.com/logs/?sessionID=${safeSessionId}`, ``
- `safeSessionId`: reused from L222. Sink: URL stored into the Allure result's `links` array (written to disk at L251). Sanitized: **Yes**.

**L238** — `` await fetch(`https://api.lambdatest.com/automation/api/v1/sessions/${safeSessionId}`, { ... ``
- `safeSessionId`: reused from L222. Sink: `fetch()` URL (PATCH request). Sanitized: **Yes**.

**L248** — `console.warn(\`[global-teardown] Failed to update status for session ${safeSessionId}\`, e);`
- `safeSessionId`: reused from L222. Sanitized: **Yes**.
- `e`: exception object. Sanitized: **None** (standard error-object logging).

**L253** — `console.log(\`[global-teardown] ✓ Added video link for "${testName}"\`);`
- `testName`: same origin as L204 (Allure result file content). Sanitized: **None**.

**L264** —
```typescript
const ltBuildId = buildId || (process.env.LT_BUILD_ID ? sanitizeId(process.env.LT_BUILD_ID) : undefined);
```
- `buildId`: already sanitized at L185.
- `process.env.LT_BUILD_ID` fallback: origin — env var (only ever set by this same process at L258 from the already-sanitized `buildId`, but could theoretically be pre-populated by an external caller). Sanitized: **Yes** — `sanitizeId(process.env.LT_BUILD_ID)`.

**L270** — `` buildUrl = `https://automation.lambdatest.com/build?build=${ltBuildId}`; ``
- `ltBuildId`: sanitized at L264 (or originally at L185). Sink: URL written to `environment.properties` (L284) and logged (L287). Sanitized: **Yes**.

**L273** — `` buildUrl = `https://automation.lambdatest.com/build?buildName=${encodeURIComponent(actualBuildName)}`; ``
- `actualBuildName`: same origin as L158. Sanitized: **Yes** — `encodeURIComponent`.

**L277–280** —
```typescript
let envContent =
  `LambdaTest.Build.URL=${buildUrl}\n` +
  `LambdaTest.Build.Name=${actualBuildName}\n` +
  (buildIdentifier ? `LambdaTest.Build.Identifier=${buildIdentifier}\n` : '') +
  (ltBuildId ? `LambdaTest.Build.ID=${ltBuildId}\n` : '') +
```
- `buildUrl`: sanitized upstream (L270/L273 above). Sanitized: **Yes** (inherited).
- `actualBuildName`: origin — `process.env.SM_LT_BUILD` + `getLambdaTestBuildName()`. Sink: written raw into `environment.properties` file content. Sanitized: **None** at this line (no `encodeURIComponent`/regex applied when writing to the properties file, unlike its URL usages at L158/L273).
- `buildIdentifier`: origin — `process.env.SM_LT_BUILD_IDENTIFIER`/`BUILD_NUMBER`/`GITHUB_RUN_NUMBER` (L39–43). Sink: written raw into `environment.properties` file content. Sanitized: **None**.
- `ltBuildId`: sanitized upstream (L264/L185). Sanitized: **Yes** (inherited).

**L284** — `fs.writeFileSync(ENV_PROPS_PATH, envContent, 'utf-8');`
- `ENV_PROPS_PATH` itself is static (see L8). `envContent` carries the values discussed above (mixed sanitized/unsanitized).

**L287** — `console.log(\`[global-teardown] Build URL: ${buildUrl}\`);`
- `buildUrl`: sanitized upstream. Sanitized: **Yes** (inherited).

**L289** — `console.warn('[global-teardown] Failed to add LambdaTest info:', error);`
- `error`: exception object (could wrap a `fetch` failure, JSON parse failure, etc.). Sanitized: **None** (standard error-object logging).

---

## 3. `e2e/utils/base-page-factory.ts` — full verbatim content

```typescript
import { BasePage } from '@gfk/gta';
import { chromium } from '@playwright/test';
import fs from 'fs';
import path from 'path';
import { WebActions } from '@gfk/gta/dist/core/web/webActions';
import { getLambdaTestBuildName, getLambdaTestSessionDir } from './configurationDetails';

/**
 * BasePageFactory: wires up a Playwright browser for tests, either
 * locally (default) or against LambdaTest cloud (when LAMBDATEST=true).
 *
 * The local/non-LambdaTest path below is unchanged from before and remains
 * the default: it only ever wires up a local browser via GTA's
 * BasePage.initializer(). storageState is READ-ONLY here: if
 * storage/auth.json exists, it is passed into contextOptions so the browser
 * reuses that session. Writing/saving storageState remains the
 * responsibility of the login page object (via webActions.getStorageState()),
 * not this factory.
 *
 * The LambdaTest path is INERT until LAMBDATEST=true and real credentials
 * (SM_LAMBDATEST_USERNAME/SM_LT_USERNAME + SM_LAMBDATEST_ACCESS_KEY/SM_LT_ACCESS_KEY) are
 * provided - see e2e/.env.example.
 */

// Store session names with timestamp as key (for reporter lookup)
export const sessionRegistry: Array<{ timestamp: number; sessionName: string; title?: string }> = [];

// Map to store session per worker - each worker runs tests sequentially
export const workerSessionMap = new Map<number, string>();

// Global current session tracker - stores the last created session
export let currentLTSession: string | null = null;

export class BasePageFactory {
  static async createBasePage(forceHeadless?: boolean): Promise<BasePage> {
    const isLambdaTest = process.env.LAMBDATEST === 'true';

    let storageState: string | undefined;
    const authPath = path.resolve(process.cwd(), 'storage/auth.json');

    if (fs.existsSync(authPath)) {
      storageState = authPath;
      console.log('Auth state loaded from storage/auth.json');
    } else {
      console.log('No auth state found - login required');
    }

    if (isLambdaTest) {
      console.log('Connecting to LambdaTest cloud...');

      const ltUser = process.env.SM_LAMBDATEST_USERNAME || process.env.SM_LT_USERNAME;
      const ltAccessKey = process.env.SM_LAMBDATEST_ACCESS_KEY || process.env.SM_LT_ACCESS_KEY;
      const ltProject = process.env.SM_LT_PROJECT || 'OMNI Discover';
      const ltBuildBase = process.env.SM_LT_BUILD || 'Discover build';
      const ltBuildIdentifier =
        process.env.SM_LT_BUILD_IDENTIFIER ||
        process.env.BUILD_NUMBER ||
        process.env.GITHUB_RUN_NUMBER ||
        undefined;
      const ltBuild = getLambdaTestBuildName(ltBuildBase, ltBuildIdentifier);

      const ltVideo = process.env.SM_LT_VIDEO === 'false' ? false : true;
      const ltNetwork = process.env.SM_LT_NETWORK === 'true';
      const ltConsole = process.env.SM_LT_CONSOLE === 'true';

      const timestamp = Date.now();
      const sessionName = `Test-${timestamp}`;

      const ltOptions: any = {
        platform: 'Windows 11',
        project: ltProject,
        build: ltBuild,
        name: sessionName,
        user: ltUser,
        accessKey: ltAccessKey,
        network: ltNetwork,
        video: ltVideo,
        console: ltConsole,
      };

      if (forceHeadless !== undefined) {
        ltOptions.headless = forceHeadless;
      }

      const wsEndpoint = `wss://cdp.lambdatest.com/playwright?capabilities=${encodeURIComponent(
        JSON.stringify({
          browserName: 'pw-chromium',
          browserVersion: 'latest',
          'LT:Options': ltOptions,
        })
      )}`;

      const browser = await chromium.connect(wsEndpoint);
      console.log(`Connected to LambdaTest (Session: ${sessionName})`);

      const contextOptions: any = {
        acceptDownloads: true,
        viewport: { width: 1920, height: 1080 },
      };

      if (storageState) {
        contextOptions.storageState = storageState;
      }

      const context = await browser.newContext(contextOptions);
      const page = await context.newPage();

      // Store session info for reporter/global-teardown
      try {
        const rawWorkerIndex = Number(process.env.TEST_WORKER_INDEX || 0);
        // Guarantee a finite numeric worker index before it is ever embedded in a filename.
        const workerIndex = Number.isFinite(rawWorkerIndex) ? rawWorkerIndex : 0;
        const testId =
          process.env.__CURRENT_TEST_ID__ || (global as any).__CURRENT_TEST_ID__ || null;
        // Sanitize testId before using it in any file path (defense-in-depth; testId is
        // already a fixed-format SHA1-hex pair from Playwright's test.id, but we sanitize
        // explicitly for SAST clarity and to match lt-session-reporter.ts).
        const safeTestId = testId ? String(testId).replace(/[^a-zA-Z0-9-]/g, '_') : null;

        const sessionDir = getLambdaTestSessionDir();
        fs.mkdirSync(sessionDir, { recursive: true });

        // store in memory
        (page as any).__LT_SESSION_NAME__ = sessionName;
        sessionRegistry.push({ timestamp, sessionName });
        workerSessionMap.set(workerIndex, sessionName);
        currentLTSession = sessionName;

        // append sessions.jsonl (fallback) - static filename, no dynamic input in the path itself
        fs.appendFileSync(
          path.join(sessionDir, 'sessions.jsonl'),
          JSON.stringify({ sessionName, timestamp, workerIndex }) + '\n',
          'utf-8'
        );

        // PRIMARY (parallel safe): write per-test session file
        if (safeTestId) {
          fs.writeFileSync(
            path.join(sessionDir, `test-${safeTestId}.json`),
            JSON.stringify({ sessionName, timestamp, testId }),
            'utf-8'
          );
          console.log(`[LT Session] ✓ test-${safeTestId}.json written`);
        } else {
          console.warn('[LT Session] Warning: testId not available, using fallback sessions.jsonl');
        }

        // optional worker file
        fs.writeFileSync(
          path.join(sessionDir, `worker-${workerIndex}.json`),
          JSON.stringify({ sessionName, timestamp }),
          'utf-8'
        );

        console.log(`[LT Session] Registered session: ${sessionName} (worker: ${workerIndex})`);
      } catch (err) {
        console.warn('[LT Session] Could not store session info:', err);
      }

      // Inject into BasePage
      (BasePage as any).browser = browser;
      (BasePage as any).context = context;
      (BasePage as any).page = page;
      (BasePage as any).webActions = new WebActions(page, context);

      return new (BasePage as any)();
    }

    const browserName = process.env.BROWSER_NAME || 'chromium';

    const envHeadless = process.env.HEADLESS !== 'false';
    const headless = forceHeadless ?? envHeadless;
    const browserOptions = { headless };

    const contextOptions = {
      acceptDownloads: true,
     // viewport: null,
      storageState,
    };

    return await BasePage.initializer(browserName, browserOptions, contextOptions);
  }
}
```

### Dynamic-value sinks in this file

**L39** — `const authPath = path.resolve(process.cwd(), 'storage/auth.json');`
- `process.cwd()`: OS-controlled, not attacker-influenced. Sink: file path used for `fs.existsSync`. Sanitized: **None applied**, not tainted.

**L43 / L45** — `console.log('Auth state loaded from storage/auth.json');` / `console.log('No auth state found - login required');`
- No dynamic value — both literals. Not a finding.

**L85–91** —
```typescript
const wsEndpoint = `wss://cdp.lambdatest.com/playwright?capabilities=${encodeURIComponent(
  JSON.stringify({
    browserName: 'pw-chromium',
    browserVersion: 'latest',
    'LT:Options': ltOptions,
  })
)}`;
```
- `ltOptions` (embeds `ltUser`, `ltAccessKey`, `ltProject`, `ltBuild`, `sessionName`, `ltNetwork`, `ltVideo`, `ltConsole`, `forceHeadless`): origin — a mix of `process.env.*` values (L51–58, L62–64) and locally computed values (`sessionName` at L67, `ltBuild` from `getLambdaTestBuildName()`). Sink: WebSocket connection URL used in `chromium.connect(wsEndpoint)` (L93) — this is the one sink in this file carrying **credentials** (`ltUser`/`ltAccessKey`) into a URL. Sanitized: **Yes, structurally** — the whole JSON payload is wrapped in `encodeURIComponent(JSON.stringify(...))`, which percent-encodes the entire query value (this is the standard/necessary LambdaTest connection mechanism; the credentials are not separately regex-sanitized, but are never logged and are transmitted only to `wss://cdp.lambdatest.com`, the intended LambdaTest endpoint).

**L94** — `console.log(\`Connected to LambdaTest (Session: ${sessionName})\`);`
- `sessionName`: origin — `` `Test-${timestamp}` `` (L67), where `timestamp = Date.now()` (L66) — generated internally by this same function, not derived from external/tainted input. Sanitized: **None applied** (not needed for an internally-generated numeric-suffixed string, but no explicit guard exists at this line either).

**L110–112** —
```typescript
const rawWorkerIndex = Number(process.env.TEST_WORKER_INDEX || 0);
// Guarantee a finite numeric worker index before it is ever embedded in a filename.
const workerIndex = Number.isFinite(rawWorkerIndex) ? rawWorkerIndex : 0;
```
- `process.env.TEST_WORKER_INDEX`: origin — Playwright-set env var for the worker process index. Sanitized: **Yes** — `Number.isFinite(rawWorkerIndex) ? rawWorkerIndex : 0` guarantees a finite number before it's used in any filename below.

**L113–118** —
```typescript
const testId =
  process.env.__CURRENT_TEST_ID__ || (global as any).__CURRENT_TEST_ID__ || null;
// Sanitize testId before using it in any file path (defense-in-depth; testId is
// already a fixed-format SHA1-hex pair from Playwright's test.id, but we sanitize
// explicitly for SAST clarity and to match lt-session-reporter.ts).
const safeTestId = testId ? String(testId).replace(/[^a-zA-Z0-9-]/g, '_') : null;
```
- `testId`: origin — `process.env.__CURRENT_TEST_ID__` / `global.__CURRENT_TEST_ID__`, both set by `lt-session-reporter.ts`'s `onTestBegin()` from Playwright's `test.id`. Sanitized: **Yes** — regex replace at L118.

**L131** — `path.join(sessionDir, 'sessions.jsonl')` (inside `fs.appendFileSync`)
- No dynamic filename component (literal `'sessions.jsonl'`), already commented `// static filename, no dynamic input in the path itself`. Not a finding. (`workerIndex`/`sessionName`/`timestamp` inside the JSON body are file *content*, not path components.)

**L139** — `path.join(sessionDir, \`test-${safeTestId}.json\`)`
- `safeTestId`: sanitized at L118 (above). Sanitized: **Yes**.

**L143** — `console.log(\`[LT Session] ✓ test-${safeTestId}.json written\`);`
- `safeTestId`: same sanitized value. Sanitized: **Yes**.

**L150** — `path.join(sessionDir, \`worker-${workerIndex}.json\`)`
- `workerIndex`: guaranteed-finite number from L112. Sanitized: **Yes**.

**L155** — `console.log(\`[LT Session] Registered session: ${sessionName} (worker: ${workerIndex})\`);`
- `sessionName`: internally generated (see L94 analysis). Sanitized: **None applied** (not needed).
- `workerIndex`: guaranteed-finite number (L112). Sanitized: **Yes** (inherited).

**L145** — `console.warn('[LT Session] Warning: testId not available, using fallback sessions.jsonl');`
- No dynamic value — literal. Not a finding.

**L157** — `console.warn('[LT Session] Could not store session info:', err);`
- `err`: exception object from the surrounding `try`. Sanitized: **None** (standard error-object logging).

---

## 4. `e2e/utils/configurationDetails.ts` — full verbatim content

```typescript
import * as path from 'path';

/**
 * Constructs the LambdaTest build name with optional build identifier suffix.
 * Used consistently across playwright.config.ts, base-page-factory.ts, and globalTeardown.ts.
 *
 * @param ltBuild - Base build name (e.g., "Discover build")
 * @param buildIdentifier - Optional identifier to append (e.g., GitHub run number)
 * @returns Formatted build name with identifier if provided
 */
export const getLambdaTestBuildName = (
  ltBuild: string,
  buildIdentifier?: string
): string => {
  return buildIdentifier ? `${ltBuild} (${buildIdentifier})` : ltBuild;
};

export const getLambdaTestSessionDir = (): string =>
  path.join(process.cwd(), 'test-results', '.lt-sessions');
```

### Dynamic-value sinks in this file

**L13–15** — `return buildIdentifier ? \`${ltBuild} (${buildIdentifier})\` : ltBuild;`
- `ltBuild`, `buildIdentifier`: function parameters — origin traced back to callers: `playwright.config.ts` (env vars), `base-page-factory.ts` (`ltBuildBase`/`ltBuildIdentifier`, from `process.env.SM_LT_BUILD`/etc.), and `globalTeardown.ts` (`ltBuild`/`buildIdentifier`, same env vars). This function itself does not log or build a file path — it only returns a formatted string, which callers then use in URLs/logs/file content (already covered under those files above). Sanitized: **None applied here** (no sink at this line itself — it's a pure string-formatting helper, not a sink).

**L18–19** — `path.join(process.cwd(), 'test-results', '.lt-sessions')`
- `process.cwd()`: OS-controlled. Sink: this is the shared session-directory path used as a base by both `lt-session-reporter.ts` and `base-page-factory.ts`. Sanitized: **None applied**, not tainted/attacker-controllable — all other path components are literals.

No `console.*` calls exist in this file.

---

## 5. `e2e/utils/resource-manager.ts` — full verbatim content

```typescript
import { PageElement } from '@gfk/gta';
import { ElementResource } from '../interfaces/iElementResource';

/**
 * To use the ResourceManager, you can create an instance and register your component resources.
 * The resources should be structured with a unique component name and an array of web elements.
 * @example
 * Import below code lines to use ResourceManager in your component. Please ensure you have given the correct path to the resource-manager file.
 * ```typescript
 * import { ResourceManager } from './resource-manager';
 * import * as sampleResources1 from '../../../object-resources/sample-resources-1.json';
 * import * as sampleResources2 from '../../../object-resources/sample-resources-2.json';
 * 
 * // Below provided sample resources structure to be added in object-resources/sample-resources.json
 * const sampleResources1 = {
 *   name: 'My Component',
 *   webElements: [
 *     {
 *       elementName: 'submitButton',
 *       selectorValue: '[data-test-id="submit-btn"]',
 *       selectorType: 'css'
 *     },
 *     {
 *       elementName: 'dynamicItem',
 *       selectorValue: '[data-item="${text}"]',
 *       selectorType: 'css'
 *     }
 *   ]
 * }
 * 
 * // Create an instance of ResourceManager with an empty registry
 * const resourceManager = new ResourceManager({});
 * 
 * // Register resources for a component
 * resourceManager.registerResources('MyComponent1', sampleResources1);
 * resourceManager.registerResources('MyComponent2', sampleResources2);
 * 
 * // Now you can retrieve resources by their names
 * const submitButton = resourceManager.getResource('submitButton', 'MyComponent');
 * const dynamicItem = resourceManager.getDynamicResource('dynamicItem', 'item123', 'MyComponent');
 */

export class ResourceManager {
    
    // Resource registry to store component resources
    private resources: Record<string, ElementResource>;

    constructor(resources: Record<string, ElementResource>) {
        this.resources = resources;
    }
    
    /**
     * Registers component resources with the registry
     * 
     * @param componentName - Unique name for the component registering resources
     * @param resources - The resources object to register
     */
    registerResources(componentName: string, resources: ElementResource): void {
        // Use this.resources instead of resourceRegistry which isn't defined
        this.resources[componentName] = resources;
    }

    /**
     * Retrieves a web element resource by its name from any registered resource object
     * 
     * @param name - The name of the web element
     * @param componentName - Optional specific component to search in
     * @returns The matching web element resource
     * @example
     * // Returns: {
     * //   elementName: "searchButton",
     * //   selectorValue: "[data-test-id='search-btn']",
     * //   selectorType: "css"
     * // }
     * getResource('searchButton', 'SearchComponent')
     * @throws Error if the element is not found
     */
    getResource(name: string, componentName?: string): PageElement {
     if (componentName) {
         if (!this.resources[componentName]) {
             throw new Error(`Component '${componentName}' is not registered.`);
        }
         const element = this.resources[componentName].webElements.find((webElement: PageElement) => webElement.elementName === name
        );

        if (element) {
             return element;
        }} else { // Otherwise search all registered resources
            for (const [component, resourceObj] of Object.entries(this.resources)) {
                const element = resourceObj.webElements.find(
                    (x: PageElement) => x.elementName === name
                );
                if (element) return element;
            }
        }

        throw new Error(
            `Web element with name '${name}' not found in the${componentName ? ` ${componentName}` : ''} object repository.`
        );
    }

    /**
     * Retrieves a web element resource by its name from the object repository with dynamic text in selector value(or locator).
     * 
     * @param name - The name of the web element.
     * @param text - The dynamic text to replace in the selector value.
     * @param componentName - Optional component to search in. If omitted, searches all components.
     * @returns The matching web element resource.
     * @example
     * // Assuming an element with selectorValue: '[data-row='${text}']'
     * // Returns: {
     * //   elementName: "dataRow",
     * //   selectorValue: "[data-row='user123']", // ${text} replaced in '[data-row='${text}']'
     * //   selectorType: "css" 
     * // }
     * getDynamicResource('dataRow', 'user123', 'TableComponent')
     * @throws Error if the element is not found.
     */
    getDynamicResource(name: string, text: string, componentName?: string): PageElement {
        if (!name) {
            throw new Error(`Invalid resource name '${name}' provided to getDynamicResource.`);
        }
        // Use this.getResource instead of undefined getResource function
        const element = this.getResource(name, componentName);
        if (!element || !element.selectorValue) {
            throw new Error(`Web element with name '${name}' does not have a valid selector.`);
        }
        if (!element.selectorValue.includes('${text}')) {
            throw new Error(`Web element with name '${name}' does not have a dynamic selector that includes '${text}'.`);
        }
        // Return the dynamic resource object with the provided text
        return {
            ...element,
            selectorValue: element.selectorValue.replace('${text}', text) // Replace ${text} with the provided text
        };
    }

    /**
     * Retrieves a web element resource with MULTIPLE dynamic replacements in the selector value.
     * Supports composite selectors with multiple placeholders (e.g., ${questionNumber}, ${segmentIndex}).
     * 
     * @param name - The name of the web element
     * @param replacements - Object with key-value pairs for dynamic replacements
     * @param componentName - Optional component to search in. If omitted, searches all components.
     * @returns The matching web element resource with all placeholders replaced
     * 
     * @example
     * // Assuming an element with selectorValue: '[data-test-id="ao-toggle-segment-q${questionNumber}-${segmentIndex}"]'
     * // Returns: {
     * //   elementName: "toggleSegment",
     * //   selectorValue: '[data-test-id="ao-toggle-segment-q1-0"]', // All placeholders replaced
     * //   selectorType: "tag"
     * // }
     * getMultiDynamicResource('toggleSegment', { questionNumber: '1', segmentIndex: '0' }, 'AssortmentOptimizerToggle')
     * 
     * @example
     * // For selectors with multiple dynamic parts
     * getMultiDynamicResource('pinMenuItem', { index: '0', action: 'text' }, 'NlsnPinMenuComponent')
     * 
     * @throws Error if element is not found or if required placeholders are missing
     */
    getMultiDynamicResource(
        name: string,
        replacements: Record<string, string | number>,
        componentName?: string
    ): PageElement {
        if (!name) {
            throw new Error(`Invalid resource name '${name}' provided to getMultiDynamicResource.`);
        }

        if (!replacements || Object.keys(replacements).length === 0) {
            throw new Error(`No replacement values provided for getMultiDynamicResource.`);
        }

        const element = this.getResource(name, componentName);

        if (!element || !element.selectorValue) {
            throw new Error(`Web element with name '${name}' does not have a valid selector.`);
        }

        // Perform replacements for all provided key-value pairs
        let updatedSelector = element.selectorValue;

        for (const [key, value] of Object.entries(replacements)) {
            const placeholder = `\${${key}}`;
            if (!updatedSelector.includes(placeholder)) {
                throw new Error(
                    `Placeholder '${placeholder}' not found in selector for element '${name}'. ` +
                    `Available selector: ${element.selectorValue}`
                );
            }
            // Use literal string replacement instead of RegExp to avoid regex injection
            // This safely handles keys with special regex characters
            updatedSelector = updatedSelector.split(placeholder).join(String(value));
        }

        // Return the resource object with all placeholders replaced
        return {
            ...element,
            selectorValue: updatedSelector
        };
    }
}
```

### Dynamic-value sinks in this file

This file contains **no `console.*` calls and no `fs`/`path` usage at all** — it only builds
in-memory `PageElement` selector objects and throws `Error` objects. It is included here for
completeness since it's the third file in `e2e/utils/`, but it has zero file-path or log sinks.
The only dynamic values that reach a sink-like construct are interpolated into thrown `Error`
messages (not `console.*`, but a "similar sensitive sink" per the investigation's scope, since
uncaught errors are frequently surfaced in logs/test output upstream):

- **L81** `throw new Error(\`Component '${componentName}' is not registered.\`);` — `componentName`: caller-supplied parameter (test/page-object code, not external/attacker input). Sanitized: **None** (not needed — this is developer-controlled input from within the same codebase, e.g. a hardcoded component name string).
- **L97–99** `throw new Error(\`Web element with name '${name}' not found in the${componentName ? \` ${componentName}\` : ''} object repository.\`);` — `name`, `componentName`: same as above, developer-controlled call-site arguments. Sanitized: **None**.
- **L121, L126, L129** (`getDynamicResource`) — `name`, `text`: `text` is the one parameter here that's explicitly documented as "dynamic" (e.g., could originate from test data), but it flows into an `Error` message string, not a path/log. Sanitized: **None**.
- **L168, L172, L178, L187–190** (`getMultiDynamicResource`) — `name`, `replacements` (values), `element.selectorValue`: same pattern — developer/test-data-supplied values interpolated into thrown `Error` messages. Sanitized: **None**. Note the explicit comment at L200–201: `// Use literal string replacement instead of RegExp to avoid regex injection` — this shows the file's author already considered and mitigated a *different* injection class (regex injection via `String.replace(RegExp)`) for the actual selector-building logic, just not for the `Error`-message sinks.

None of these reach a file path or `console.*` call, so they are lower priority than the findings in the other four files, but are documented here per the "check every file" instruction.

---

## Summary table

| File | Line(s) | Dynamic Value | Source | Sanitized? | Sanitization Code |
|---|---|---|---|---|---|
| lt-session-reporter.ts | 31 | `test.title`, `test.id` | Playwright `TestCase` | No | — |
| lt-session-reporter.ts | 42 | `safeTestId` | `test.id` | Yes | `testId.replace(/[^a-zA-Z0-9-]/g, '_')` (L39) |
| lt-session-reporter.ts | 49–50 | `data.sessionName` | File content (`test-*.json` written by base-page-factory.ts) | No | — |
| lt-session-reporter.ts | 65 | `workerIndex` | `result.parallelIndex` | No (not needed; not a path/log sink here) | — |
| lt-session-reporter.ts | 69–79 | `s.workerIndex`, `s.timestamp`, `s.sessionName` | File content (`sessions.jsonl`) | No | — |
| lt-session-reporter.ts | 91 | `err` | Caught exception | No | — |
| lt-session-reporter.ts | 95 | `test.title`, raw `testId` | Playwright `TestCase` | No | — |
| lt-session-reporter.ts | 105 | `normalizedFullName`, `sessionName` | `test.titlePath()`; file content | No | — |
| lt-session-reporter.ts | 113 | `safeTestId`, `process.pid`, `Date.now()` | Sanitized above; Node PID; timestamp | Yes (safeTestId); N/A (pid/timestamp are numeric) | `testId.replace(/[^a-zA-Z0-9-]/g, '_')` |
| lt-session-reporter.ts | 130 | `mappingFile` | Built at L113 | Yes | `path.basename(mappingFile)` |
| lt-session-reporter.ts | 132 | `err` | Caught exception | No | — |
| lt-session-reporter.ts | 152 | `this.testSessions.size` | In-memory Map | N/A (not tainted) | — |
| lt-session-reporter.ts | 154 | `err` | Caught exception | No | — |
| globalTeardown.ts | 6 | `process.cwd()` | Node process cwd | N/A (not tainted) | — |
| globalTeardown.ts | 65 | `mappingFiles.length` | `fs.readdirSync` count | N/A | — |
| globalTeardown.ts | 70, 84 | `file` → `sanitizedFile` | `fs.readdirSync(sessionDir)` | Yes | `path.basename(file)` |
| globalTeardown.ts | 88 | `sessionDir` | `getLambdaTestSessionDir()` | N/A (not tainted) | — |
| globalTeardown.ts | 101–102 | `file` → `sanitizedFile` | `fs.readdirSync(ALLURE_RESULTS_DIR)` | Yes | `path.basename(file)` |
| globalTeardown.ts | 141 | `fullName`, `sessionName` | Allure result JSON file content; mapping-file content | No | — |
| globalTeardown.ts | 144 | `fullName` | Allure result JSON file content | No | — |
| globalTeardown.ts | 155 | `ltUser`, `ltAccessKey` | `process.env.SM_LAMBDATEST_*` | None (never logged/path'd; used only to build Basic-auth token) | — |
| globalTeardown.ts | 158 | `actualBuildName` | `process.env.SM_LT_BUILD` etc. via `getLambdaTestBuildName()` | Yes | `encodeURIComponent(actualBuildName)` |
| globalTeardown.ts | 181–186 | `sessionBuildId` → `buildId` | External LambdaTest API response | Yes | `sanitizeId(sessionBuildId)` |
| globalTeardown.ts | 204 | `testName` | Allure result JSON file content | No | — |
| globalTeardown.ts | 208 | `sessionName` | Mapping-file content (via Allure `LT_SESSION` param) | No | — |
| globalTeardown.ts | 214–217 | `s.name` → `safeAvailableNames`; `sessionName` | External LambdaTest API response; mapping-file content | Yes (names); No (sessionName) | `sanitizeId(s.name)` |
| globalTeardown.ts | 222–223 | `session.session_id` → `safeSessionId` | External LambdaTest API response | Yes | `sanitizeId(session.session_id)` |
| globalTeardown.ts | 233 | `safeSessionId` | Reused from L222 | Yes | (inherited) |
| globalTeardown.ts | 238 | `safeSessionId` | Reused from L222 | Yes | (inherited) |
| globalTeardown.ts | 248 | `safeSessionId`, `e` | Reused from L222; caught exception | Yes (id); No (`e`) | (inherited) |
| globalTeardown.ts | 253 | `testName` | Allure result JSON file content | No | — |
| globalTeardown.ts | 264 | `process.env.LT_BUILD_ID` fallback | Env var (self-set from sanitized `buildId`) | Yes | `sanitizeId(process.env.LT_BUILD_ID)` |
| globalTeardown.ts | 270 | `ltBuildId` | Sanitized above | Yes | (inherited) |
| globalTeardown.ts | 273 | `actualBuildName` | Same as L158 | Yes | `encodeURIComponent(actualBuildName)` |
| globalTeardown.ts | 278 | `actualBuildName` (written to file content) | `process.env.SM_LT_BUILD` etc. | **No** | — |
| globalTeardown.ts | 279 | `buildIdentifier` (written to file content) | `process.env.SM_LT_BUILD_IDENTIFIER`/`BUILD_NUMBER`/`GITHUB_RUN_NUMBER` | **No** | — |
| globalTeardown.ts | 287 | `buildUrl` | Sanitized above | Yes | (inherited) |
| globalTeardown.ts | 289 | `error` | Caught exception | No | — |
| base-page-factory.ts | 39 | `process.cwd()` | Node process cwd | N/A (not tainted) | — |
| base-page-factory.ts | 85–91 | `ltOptions` (incl. `ltUser`, `ltAccessKey`) | `process.env.SM_LAMBDATEST_*` etc. | Yes (structurally, via `encodeURIComponent(JSON.stringify(...))`) | `encodeURIComponent(JSON.stringify({...}))` |
| base-page-factory.ts | 94 | `sessionName` | Internally generated `` `Test-${Date.now()}` `` | None applied (not tainted) | — |
| base-page-factory.ts | 110–112 | `rawWorkerIndex` → `workerIndex` | `process.env.TEST_WORKER_INDEX` | Yes | `Number.isFinite(rawWorkerIndex) ? rawWorkerIndex : 0` |
| base-page-factory.ts | 113–118 | `testId` → `safeTestId` | `process.env.__CURRENT_TEST_ID__` / `global.__CURRENT_TEST_ID__` | Yes | `String(testId).replace(/[^a-zA-Z0-9-]/g, '_')` |
| base-page-factory.ts | 139 | `safeTestId` | Sanitized above | Yes | (inherited) |
| base-page-factory.ts | 143 | `safeTestId` | Sanitized above | Yes | (inherited) |
| base-page-factory.ts | 150 | `workerIndex` | Sanitized above | Yes | (inherited) |
| base-page-factory.ts | 155 | `sessionName`, `workerIndex` | Internally generated; sanitized above | None (sessionName, not needed) / Yes (workerIndex) | (inherited) |
| base-page-factory.ts | 157 | `err` | Caught exception | No | — |
| configurationDetails.ts | 13–15 | `ltBuild`, `buildIdentifier` | Function parameters (caller env vars) | N/A (pure formatter, not a sink itself) | — |
| configurationDetails.ts | 18–19 | `process.cwd()` | Node process cwd | N/A (not tainted) | — |
| resource-manager.ts | 81, 97–99, 121–190 (various) | `name`, `componentName`, `text`, `replacements` values | Caller-supplied parameters (developer/test code) | No (but not path/log sinks — only thrown `Error` messages) | — |

---

## Notes on scope

- "Sanitized: No" does **not** necessarily mean exploitable — several of these values (e.g.
  `test.id`, `sessionName` generated as `` `Test-${Date.now()}` ``, Node's own `process.pid`)
  are structurally constrained and not attacker-controllable in this test-automation context.
  This document reports sanitization **presence/absence exactly as requested**, independent of
  actual exploitability, so it can be cross-referenced against CodeQL/Cycode alerts.
- The two lines with the clearest remaining gap are `globalTeardown.ts` **L278** and **L279**
  (`actualBuildName` / `buildIdentifier` written raw into `environment.properties`), since the
  same values are sanitized (`encodeURIComponent`) everywhere else they're used as URLs in the
  same file, but not when written as file content.
