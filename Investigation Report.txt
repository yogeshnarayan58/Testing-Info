Summary of changes
1. start-from-scratch.spec.ts:18

   test('All dimensions selection', { tag: '@StartFromScratch' }, async () => {
-    test.setTimeout(300000);
     const basePage = await BasePageFactory.createBasePage();
2. start-with-prompt.spec.ts:15-16

-test.describe('Start with a prompt', () => {
-  test('All dimensions selection', { tag: '@StartWithPrompt' }, async () => {
+test.describe('Start with a prompt', { tag: '@StartWithPrompt' }, () => {
+  test('All dimensions selection', { tag: '@StartWithPrompt' }, async () => {
(the test's tag was already @StartWithPrompt, so it just needed to be duplicated onto describe; test.setTimeout(300000) in this file was untouched since only the from-scratch spec's timeout was flagged)

3. dev-test-data.json:60-92 — added a new sibling dataset "US Homescan Panel 2yrs Total" with its own searchTerm: "Homescan", datasetIndex: 0, and Homescan-appropriate dimensionSelections (grocery category, household-income demographic slice, etc.), matching the exact shape of the existing entry.

Important discrepancy found — please confirm: your request described the existing dataset entries as having sourceType, dataView, navUrlWithDataSetId fields "etc." Those fields don't actually exist anywhere in this repo's dev-test-data.json or in searchAndSelectDataset() — that field vocabulary belongs to the other workspace folder (disc-fe-omnishopper-ui-automation), a different project. I built the new entry using this repo's actual shape (searchTerm, datasetIndex, dimensionSelections) instead of inventing unused fields.

On the navigationURL trailing-slash question: common.navigationURL ("survey") is not read by any code — navigateToSurvey() in both page objects hardcodes the literal string 'survey' via new URL('survey', appBaseURL) rather than reading from the JSON. Since it's dead/unused data, a trailing slash wouldn't change any behavior either way. I left it unchanged; flag if you want it removed entirely as unused, or if some other consumer actually reads it that I haven't found.

Type-check: npx tsc --noEmit in e2e completed with no output — no errors introduced