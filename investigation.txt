# Wider Search: `btn-next-dataset` / `apply-dataset-btn` in App Source

Scope of this search (exact-string, case-sensitive):
- `libs/**/*.html`, `libs/**/*.ts` (all sub-libs under `libs/`, not just `survey-mod-lib`)
- `apps/**/*.html`, `apps/**/*.ts` (if the folder exists)
- `src/**/*.html`, `src/**/*.ts` (repo-root `src/`, i.e. the `survey-mod-app` dev-preview harness)

Explicitly excluded: `node_modules/`, `dist/`, `.angular/`, `coverage/`, any other
build-output folder, and the `e2e/` folder itself.

## Folder inventory checked

- `libs/` contains a single sub-library: `libs/survey-mod-lib/` — no other libs exist.
- `apps/` — **does not exist** in this repo (confirmed via directory listing of repo root).
- `src/` — exists at repo root (`survey-mod-app`, the local `ng serve` dev-preview harness).

## Results

### `btn-next-dataset`

**Zero results.** No file under `libs/**/*.html`, `libs/**/*.ts`, or
`src/**/*.html`, `src/**/*.ts` contains the exact string `btn-next-dataset`.

### `apply-dataset-btn`

**Zero results.** No file under `libs/**/*.html`, `libs/**/*.ts`, or
`src/**/*.html`, `src/**/*.ts` contains the exact string `apply-dataset-btn`.

## Conclusion

Across all in-scope app source locations (`libs/`, `apps/` [absent], and root
`src/`), neither string appears anywhere. This confirms the earlier Phase 1
finding was not an artifact of a narrow search path — the two `data-test-id`
values are not present in any first-party template or TypeScript source file in
this repository, in any lib or app. (No node_modules/dist/build-output search
was performed or reported here, per scope.)
